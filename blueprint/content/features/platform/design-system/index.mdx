---
title: Design System
description: The shared visual and interaction layer — color, elevation and depth tokens, and the overlay rules in its sub-capability.
---

The design system is the layer the App and Landing surfaces share for appearance and interaction.

## Current behavior

- Color, elevation and depth tokens, and the rules for the app's background layers, are documented in `docs/design-system.md`.
- Dialog ownership, dialog controls, and how a dialog closes are described in the Overlays sub-capability.

## Known gaps

- The token documentation lives in `docs/design-system.md` rather than in Blueprint. Moving it here is a separate decision; until then this page only points to it.
