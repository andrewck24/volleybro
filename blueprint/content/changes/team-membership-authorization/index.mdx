---
title: Overview
---

import { FileTour } from "@/components/FileTour";
import { TLDR } from "@/components/TLDR";

<TLDR>
  程式裡的「成員」包含了還沒接受邀請的人，管理員可以把擁有者降級或刪除，還有三個讀取端點根本不問呼叫者是誰。本
  Change
  讓「成員」只指已加入的球員，讓角色只屬於受邀者與成員，保護擁有者，並讓隊伍資訊、名單與單一球員資料在隱私設定上線前只給成員讀取。
</TLDR>

## Context

規則是：在隱私設定上線前，隊伍資訊、隊伍名單與成員資訊只給成員查看，受邀者不是成員。詞彙依 `CONTEXT.md`：未連結球員、受邀者、成員；角色由高到低為擁有者、管理員、一般成員。

### 讀取端點與授權的缺口

| #   | 位置                                                         | 誰讀得到或做得到                                                   | 影響                                         |
| --- | ------------------------------------------------------------ | ------------------------------------------------------------------ | -------------------------------------------- |
| 1   | `GET /api/teams/{teamId}`                                    | 任何人，不需登入：route 沒有 `withAuth`，`GetTeamUseCase` 沒有授權 | 隊名、`lineups`（含 player id）、stats       |
| 2   | `GET /api/players/{playerId}`                                | 任何登入者：route 忽略 `userId`，`GetPlayerUseCase` 沒有授權       | 單一球員的完整資料，含 `email`               |
| 3   | `GET /api/teams/{teamId}/players`                            | 任何登入者：route 忽略 `userId`，`GetTeamPlayersUseCase` 沒有授權  | 整隊名單，每筆含 `email`、`userId`、`role`   |
| 4   | `AuthorizationService.verifyTeamRole` 與 `verifyIsTeamAdmin` | 已連結帳號的受邀者：只看 `role`、不看 `status`                     | 通過全部成員與管理員檢查，包含寫入路徑       |
| 5   | `update-role`、`remove-player`                               | 任何管理員：只檢查呼叫者，不檢查目標                               | 可以把擁有者降為管理員或刪除，隊伍失去擁有者 |

第 4 項是「成員」這個詞本身的定義。`role` 無法分辨三種球員：`create-player` 建立未連結球員時一律寫入 `MEMBER`，離開隊伍、拒絕與取消邀請都不清除 `role`。先被邀請、之後才註冊的使用者，會在註冊時被連結 `userId` 而 `status` 仍是 `INVITED`；這樣的受邀者可以開賽、記錄 Rally、改陣容，被邀請為管理員者在接受前就能新增或刪除球員。`transfer-ownership` 以 `userId` 是否存在判斷新擁有者已加入，所以擁有權可以移轉給尚未接受邀請的人。

`GET /api/games` 與 `GET /api/games/{gameId}` 的 route 同樣沒有 `withAuth`，但它們的 use case 內有 `verifySession` 與 `verifyTeamRole`，只受第 4 項影響。

### 邀請與資料

- **邀請已註冊的使用者，對方永遠收不到。** `create-invitation` 只寫入 `email`，而受邀者的邀請清單以 `userId` 查詢，連結 `userId` 只在註冊時發生一次。
- **一位使用者在同一隊可能出現兩位球員。** `(teamId, userId)` 只有一般索引；一旦邀請時會連結帳號，對同一個人再發一次邀請就會產生第二位球員，而 `findByTeamIdAndUserId` 以 `findOne` 隨機取得其中一位。
- **production 有 32 份球員文件沒有 `status`。** 2026-02 的 unify-player 遷移曾對所有 player `$unset` status，2026-03 的 player-invitations 只加了 schema 預設值 `NONE`，沒有回填。
- **`create-player` 帶 email 時建立的是帶 `email` 的未連結球員**，違反 `validatePlayerStatus`，也永遠不會被連結。

## Goals

- 「成員」在整個 repo 只有一個定義，且可以不經 mock 直接測試；角色只屬於受邀者與成員。
- 受邀者在接受邀請前通不過任何成員或管理員檢查；擁有者不能被他人降級或刪除。
- 非成員（含未登入）讀不到上表任何讀取端點的資料，進入隊伍頁面時看到錯誤狀態而不是崩潰。
- 邀請已註冊的使用者時，對方看得到邀請、看得到隊名，可以接受或拒絕。
- 一位使用者在同一隊至多一位球員，由資料庫保證。
- 介面與文件的用詞與 `CONTEXT.md` 一致：擁有者不再稱為「隊長」，一般成員不再稱為「成員」，刪除球員不再稱為「移除成員」。

## Non-goals

- 不改變回應欄位的可見範圍。「email 只回傳給擁有者與管理員」屬於球員資訊頁會籍區塊的工作。
- 不新增「解除帳號連結」動作。它與會籍區塊的重做一起處理；本 Change 只把現有的刪除球員改名並套用授權規則。
- 不改變擁有者移轉的生效方式。移轉是否需要對方接受另案處理。
- 不實作隱私設定，也不提供「擁有者可以關閉管理員互相管理」的隊伍設定。本 Change 是隱私設定上線前的預設規則；隱私設定的 roadmap 列在 Archive 時加入 Features。
- 不拆出 Membership entity，不採用 better-auth organization plugin。理由在 D1。
- 不刪除 `verifyIsTeamOwner`、`verifyPlayerRole` 與 `getPlayerRole`。三者在 production 程式碼中沒有呼叫者，但刪除需要另外確認。
- 不改名比賽紀錄中的「替補」按鈕與 `game.ts` 的 `Player`／`Team` 型別。它們屬於比賽紀錄詞彙的後續工作。

## Change boundaries

- **在內**：Player entity 的 union 型別、成員與角色判斷、repository 的縮窄、`AuthorizationService` 改為委派；`create-player`、`leave-team`、`reject-invitation`、`cancel-invitation` 依模型不再留下角色；`transfer-ownership` 的擁有者與目標檢查。
- **在內**：D3 的角色管理規則套用到 `update-role` 與刪除球員。
- **在內**：邀請時連結已註冊的帳號、邀請衝突檢查、`(teamId, userId)` 部分唯一索引、既有資料的 audit 與 migrate 腳本。
- **在內**：三個讀取端點的 use case 授權；受邀者球員清單帶上 `teamName`；`AcceptInvitationUseCase` 回傳 Player 讓 controller 不再經過 `GetPlayerUseCase`。
- **在內**：非成員進入隊伍頁面時的錯誤狀態，以及刪除成員時清除被刪除者的 `activeTeamId`。
- **在內**：介面文案與角色標籤依 `CONTEXT.md` 改正。
- **不在內**：`GET /api/users?email=` 以 email 反查使用者，歸在隱私設定主題。

## Capability impact

`team-management/membership` 取得三條約束：**成員是已加入的球員，受邀者與未連結球員都不是成員**；**一位使用者在同一隊至多一位球員**；**邀請已註冊的使用者時直接連結帳號**。

`team-management/roles` 取得兩條約束：**角色只屬於受邀者與成員**；**擁有者與管理員可以管理管理員以下的人，擁有者不能被他人降級或刪除，只能透過移轉離開這個角色**。

## Impact

<FileTour
  files={[
    {
      path: "src/entities/player.ts",
      change: "modified",
      summary:
        "Player 改為 UnlinkedPlayer、InvitedPlayer、TeamMember 的 union；新增 isTeamMember、hasTeamRole 與比較呼叫者與目標的角色管理判斷。成員與角色的規則只存在這裡。",
    },
    {
      path: "src/infrastructure/db/repositories/player.repository.mongo.ts",
      change: "modified",
      summary:
        "讀取時把文件縮窄成三種球員之一，不符合模型的文件讀取失敗而不是被當成未連結球員。",
    },
    {
      path: "src/infrastructure/db/mongoose/schemas/player.ts",
      change: "modified",
      summary: "(teamId, userId) 改為只對有 userId 的文件生效的部分唯一索引。",
    },
    {
      path: "scripts/migrations/team-membership.audit.ts",
      change: "added",
      summary:
        "只讀取：回報每一類不符合新模型的文件數量與 (teamId, userId) 的重複組合。",
    },
    {
      path: "scripts/migrations/team-membership.migrate.ts",
      change: "added",
      summary:
        "可重跑：有重複即停止；否則回填 status、清除未連結球員的 role 與 email、連結舊邀請的帳號，最後建立唯一索引。",
    },
    {
      path: "src/infrastructure/services/auth/authorization.service.ts",
      change: "modified",
      summary:
        "verifyTeamRole 查詢後委派給 entity 函式；verifyIsTeamAdmin 委派給 verifyTeamRole。",
    },
    {
      path: "src/applications/usecases/player/create-invitation.usecase.ts",
      change: "modified",
      summary:
        "email 對應已註冊的使用者時寫入 userId；該使用者在隊上已有球員時回傳衝突錯誤。",
    },
    {
      path: "src/applications/usecases/player/update-role.usecase.ts",
      change: "modified",
      summary:
        "目標必須是受邀者或成員，且呼叫者可以管理目標的角色；擁有者不是目標。",
    },
    {
      path: "src/applications/usecases/player/remove-player.usecase.ts",
      change: "modified",
      summary:
        "刪除球員套用同一條管理規則；被刪除者的 activeTeamId 若指向該隊則清除。",
    },
    {
      path: "src/applications/usecases/player/transfer-ownership.usecase.ts",
      change: "modified",
      summary: "現任擁有者與新擁有者都經過成員判斷；受邀者不能成為擁有者。",
    },
    {
      path: "src/applications/usecases/team/get-team.usecase.ts",
      change: "modified",
      summary: "要求呼叫者是該隊成員。route 補上 withAuth 並傳入 userId。",
    },
    {
      path: "src/applications/usecases/player/get-player.usecase.ts",
      change: "modified",
      summary: "要求呼叫者是該球員所屬隊伍的成員。route 傳入 userId。",
    },
    {
      path: "src/applications/usecases/player/get-team-players.usecase.ts",
      change: "modified",
      summary: "要求呼叫者是該隊成員。route 傳入 userId。",
    },
    {
      path: "src/applications/usecases/player/accept-invitation.usecase.ts",
      change: "modified",
      summary:
        "回傳已讀到的 Player；invitation.controller 用它取得 teamId，不再經過成員限定的 GetPlayerUseCase。",
    },
    {
      path: "src/applications/usecases/player/get-user-players.usecase.ts",
      change: "modified",
      summary: "每一筆帶上 teamName，受邀者不必讀取隊伍端點就能看到隊名。",
    },
    {
      path: "src/components/user/invitations/index.tsx",
      change: "modified",
      summary:
        "隊名改讀 teamName，不再呼叫 useTeam；邀請項目在接受前不連到隊伍頁。",
    },
    {
      path: "src/components/team/info/index.tsx",
      change: "modified",
      summary:
        "隊伍或名單讀取失敗時顯示錯誤狀態，不再以 team! 與 players! 假設資料存在。",
    },
    {
      path: "src/components/team/players/membership-section.tsx",
      change: "modified",
      summary:
        "「移除成員」改為「刪除球員」、「隊長」改為「擁有者」；刪除入口只對呼叫者可以管理的球員顯示。",
    },
    {
      path: "src/lib/constants/labels.ts",
      change: "modified",
      summary:
        "角色標籤統一為擁有者、管理員、一般成員；role-select.tsx 改用同一份標籤，不再各自定義。",
    },
    {
      path: "src/lib/api/error-messages.ts",
      change: "modified",
      summary:
        "擁有者相關錯誤文案的「隊長」改為「擁有者」，並補上角色管理規則的錯誤文案。",
    },
  ]}
/>
