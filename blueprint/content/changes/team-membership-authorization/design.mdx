---
title: Design
---

import { DecisionTimeline } from "@/components/DecisionTimeline";
import { RiskTable } from "@/components/RiskTable";
import { TLDR } from "@/components/TLDR";
import aMemberHasJoined from "./design/decisions/D1-a-member-has-joined.json";
import teamReadsAreMembersOnly from "./design/decisions/D2-team-reads-are-members-only.json";
import adminsManagePeersOwnerIsProtected from "./design/decisions/D3-admins-manage-peers-owner-is-protected.json";
import backfillBeforeTheModelTightens from "./design/decisions/D4-backfill-before-the-model-tightens.json";

<TLDR>
  型別說出每種球員有什麼，判斷與查詢分開。「誰是成員、角色夠不夠、能不能管這個人」是沒有
  I/O 的業務規則，放在 entity；「這個使用者在這隊是哪一位球員」要查資料庫，留在
  infrastructure；「查、判斷、不通過就丟錯」由 use case 呼叫 service
  完成。資料在部署前正規化、部署後才連結帳號，任何時刻都不會有人多出權限。
</TLDR>

## Adopted shape：三種球員

Player 仍是一份文件，欄位與現在相同；entity 的型別依 `status` 區分，每一種只帶自己成立的欄位。

```ts
type PlayerBase = {
  id: string;
  name: string;
  number?: number;
  position?: Position;
  teamId?: string;
  createdAt: Date;
  updatedAt: Date;
};

export type UnlinkedPlayer = PlayerBase & { status: PlayerStatus.NONE };

export type InvitedPlayer = PlayerBase & {
  status: PlayerStatus.INVITED;
  role: PlayerRole;
} & ({ userId: string; email?: never } | { email: string; userId?: never });

export type TeamMember = PlayerBase & {
  status: PlayerStatus.JOINED;
  userId: string;
  role: PlayerRole;
};

export type Player = UnlinkedPlayer | InvitedPlayer | TeamMember;
```

`isTeamMember` 是 type guard，只看 `status`；通過之後 `userId` 與 `role` 由型別保證存在，`hasTeamRole` 不需要 `player.role!`。repository 的 `toPlayer` 負責把資料庫文件縮窄成三者之一，不符合的文件拋出錯誤，而不是被 Mongoose 的預設值悄悄當成未連結球員。

一筆不符合模型的文件會讓整隊的 `findByTeamId` 與相關使用者的 `findByUserId` 讀取失敗，因為 repository 對整批文件逐一縮窄。所以模型收緊的那一刻，所有寫入路徑必須已經只寫出合法的文件，既有資料也必須已經正規化：寫入端的限縮與模型在同一個切片，正規化在部署之前完成。

| 動作                   | 之前                                  | 之後                                                     |
| ---------------------- | ------------------------------------- | -------------------------------------------------------- |
| 建立球員（不帶 email） | 未連結球員，`role` 預設 `MEMBER`      | 未連結球員，沒有角色                                     |
| 建立球員（帶 email）   | 帶 `email` 的未連結球員，永遠不會連結 | 受邀者，套用邀請的連結與衝突規則                         |
| 發出邀請               | 只寫入 `email`                        | email 已註冊則寫入 `userId`，否則寫入 email              |
| 接受邀請               | 受邀者 → 成員                         | 不變                                                     |
| 拒絕、取消邀請         | 轉為未連結球員，`role` 保留           | 轉為未連結球員，清除 `role`                              |
| 離開隊伍               | 轉為未連結球員，`role` 保留           | 轉為未連結球員，清除 `role`                              |
| 變更角色               | 任何球員皆可                          | 只限受邀者與成員，套用 D3 的管理規則                     |
| 刪除球員               | 刪除文件                              | 套用 D3 的管理規則；被刪除的成員若以該隊為預設隊伍則清除 |

## Adopted shape：一次判斷，三層分工

<svg
  viewBox="0 0 900 640"
  role="img"
  aria-label="授權的三層分工，由上而下。最上層是 application 層的 use case，GetTeamUseCase、GetPlayerUseCase、GetTeamPlayersUseCase 與其餘既有呼叫點，呼叫 AuthorizationService 的 verifyTeamRole。中間是 infrastructure 層的 AuthorizationService：查出球員、交給 entity 判斷、不通過就丟出 NOT_TEAM_MEMBER 或 INSUFFICIENT_ROLE。最下層是 entity 層的兩個純函式：isTeamMember 判斷 status 為 JOINED，hasTeamRole 以 OWNER、ADMIN、MEMBER 的階層比較角色；沒有 I/O，不經 mock 就能測試。"
  preserveAspectRatio="xMidYMid meet"
  style={{ aspectRatio: "900 / 640", width: "100%", height: "auto" }}
>
  <defs>
    <marker id="tma-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="var(--muted-foreground)" />
    </marker>
  </defs>
  <g fontFamily="ui-sans-serif, system-ui, sans-serif">
    <rect x="20" y="20" width="860" height="160" rx="12" fill="var(--card)" stroke="var(--border)" />
    <text x="48" y="62" fontSize="24" fill="var(--foreground)">Use case</text>
    <text x="860" y="62" textAnchor="end" fontSize="20" fill="var(--muted-foreground)">Application</text>
    <text x="48" y="104" fontSize="21" fontFamily="ui-monospace, monospace" fill="var(--muted-foreground)">GetTeamUseCase · GetPlayerUseCase</text>
    <text x="48" y="136" fontSize="21" fontFamily="ui-monospace, monospace" fill="var(--muted-foreground)">GetTeamPlayersUseCase</text>
    <text x="48" y="166" fontSize="20" fill="var(--muted-foreground)">＋ 其餘既有呼叫點，程式碼不變</text>

    <line x1="450" y1="180" x2="450" y2="226" stroke="var(--muted-foreground)" strokeWidth="2" markerEnd="url(#tma-arrow)" />
    <text x="470" y="210" fontSize="20" fontFamily="ui-monospace, monospace" fill="var(--muted-foreground)">verifyTeamRole(teamId, userId, role)</text>

    <rect x="20" y="230" width="860" height="160" rx="12" fill="var(--card)" stroke="var(--border)" />
    <text x="48" y="272" fontSize="24" fill="var(--foreground)">AuthorizationService</text>
    <text x="860" y="272" textAnchor="end" fontSize="20" fill="var(--muted-foreground)">Infrastructure</text>
    <text x="48" y="314" fontSize="21" fill="var(--muted-foreground)">1. 查出球員　2. 交給 entity 判斷　3. 不通過就丟錯</text>
    <text x="48" y="356" fontSize="21" fontFamily="ui-monospace, monospace" fill="var(--destructive)">NOT_TEAM_MEMBER · INSUFFICIENT_ROLE</text>

    <line x1="450" y1="390" x2="450" y2="436" stroke="var(--muted-foreground)" strokeWidth="2" markerEnd="url(#tma-arrow)" />
    <text x="470" y="420" fontSize="20" fontFamily="ui-monospace, monospace" fill="var(--muted-foreground)">Player | null</text>

    <rect x="20" y="440" width="860" height="180" rx="12" fill="var(--card)" stroke="var(--primary)" strokeWidth="2" />
    <text x="48" y="482" fontSize="24" fill="var(--foreground)">成員判斷</text>
    <text x="860" y="482" textAnchor="end" fontSize="20" fill="var(--primary)">Entity</text>
    <text x="48" y="524" fontSize="21" fontFamily="ui-monospace, monospace" fill="var(--foreground)">isTeamMember</text>
    <text x="260" y="524" fontSize="21" fill="var(--muted-foreground)">status 為 JOINED，縮窄為 TeamMember</text>
    <text x="48" y="562" fontSize="21" fontFamily="ui-monospace, monospace" fill="var(--foreground)">hasTeamRole</text>
    <text x="260" y="562" fontSize="21" fill="var(--muted-foreground)">OWNER ⊇ ADMIN ⊇ MEMBER</text>
    <text x="48" y="602" fontSize="20" fill="var(--primary)">沒有 I/O，不經 mock 就能測試；隱私設定的 relation 推導建立在同一組函式上</text>

  </g>
</svg>

## 角色管理

依 D3，呼叫者與目標的關係決定能否變更角色或刪除球員。規則以角色階層、「擁有者受保護」與「不能以自己為目標」表達，與成員判斷放在同一組 entity 函式。

| 呼叫者   | 可以變更角色的目標           | 可以刪除的目標                           |
| -------- | ---------------------------- | ---------------------------------------- |
| 擁有者   | 管理員、一般成員、受邀者     | 管理員、一般成員、受邀者、未連結球員     |
| 管理員   | 其他管理員、一般成員、受邀者 | 其他管理員、一般成員、受邀者、未連結球員 |
| 一般成員 | 無                           | 無                                       |

- 擁有者是「角色為擁有者的成員」，不是任何人變更角色或刪除的目標；擁有權只能透過移轉離開。
- 任何人都不能以自己的球員為目標。要離開隊伍走「離開隊伍」，名單上的那一格保留為未連結球員。
- 未連結球員沒有角色，不是變更角色的目標。
- 可以指定的角色仍只有管理員與一般成員，擁有者只能透過移轉產生。
- 目標是擁有者時回傳新的錯誤原因，文案說明必須由擁有者移轉；目標是自己時回傳另一個錯誤原因，文案指向離開隊伍；目標超出呼叫者的管理範圍時回傳 `INSUFFICIENT_ROLE`。

## 邀請與唯一性

- **先授權，再查詢**：發出邀請或建立帶 email 的球員時，先確認呼叫者是該隊管理員以上，之後才查詢 email 與做衝突檢查。反過來做，任何登入者都能問出某個 email 是否在某一隊的名單上。
- **連結帳號**：email 去除空白後，以 collation `strength: 2` 做不分大小寫的**精確**比對查詢使用者，不使用 `$regex`，否則 `.` 會匹配任意字元而連到別人的帳號。恰好找到一位時寫入 `userId`、不寫 email；找不到時寫入轉為小寫的 email，對方註冊時由 `linkUserToInvitations` 連結，而它以同一種不分大小寫的方式比對——邀請存的是小寫，註冊用的 email 卻可能帶大寫，精確比對會讓這種邀請永遠送不到；找到多位（大小寫不同的重複帳號）時回傳衝突，不猜測是哪一位。
- **衝突**：找到的使用者在該隊已有球員（受邀者或成員），或未註冊的 email 在該隊已有邀請時，回傳新的衝突錯誤原因；文案指出他已在名單上。
- **唯一性由索引保證**：`(teamId, userId)` 改為只對 ObjectId 型別 `userId` 生效的部分唯一索引；`(teamId, email)` 的無效宣告修正為只對字串型別 email 生效的部分唯一索引。資料庫的唯一性違反目前已轉譯為 409，改為對應到上述衝突錯誤原因。
- **註冊時的連結不會產生重複**：`(teamId, email)` 唯一，所以同一個 email 在一隊至多一筆邀請；一個帳號只註冊一次，也不會與自己已有的球員同隊。

## 資料的兩個階段

依 D4，既有資料分兩個階段處理。第一階段在部署之前，**不為任何文件新增 `userId`**，因此舊的權限檢查下沒有人多出權限；第二階段在部署之後，新的成員判斷已經上線，連結帳號才不會讓受邀者取得權限。

### 第一階段：部署前正規化

| 既有文件                       | 正規化結果                           |
| ------------------------------ | ------------------------------------ |
| 缺 `status`，有 `userId`       | 成員（`JOINED`），清除 `email`       |
| 缺 `status`，只有 `email`      | 未連結球員，清除 `role` 與 `email`   |
| 缺 `status`，兩者皆無          | 未連結球員，清除 `role`              |
| 未連結球員帶 `role` 或 `email` | 清除 `role` 與 `email`               |
| 成員帶 `email`                 | 清除 `email`                         |
| 受邀者的 `email` 帶大寫        | 轉為小寫                             |
| 任何欄位為 `null` 或空字串     | 視為缺少；`email` 與 `role` 一併清除 |

寫入任何文件之前先檢查下列情況；出現任一即停止、不寫入，列出文件交由開發者處理：

1. `(teamId, userId)` 重複。
2. 同隊受邀者的 email 在轉小寫並去除空白後重複。
3. 非成員帶擁有者角色，或某隊的擁有者成員數量不為 1。
4. 受邀者或成員缺角色；受邀者同時有、或同時缺 `userId` 與 `email`；成員缺 `userId`；未連結球員帶 `userId`。
5. `userId` 或 `teamId` 不是 ObjectId，缺 `teamId`，或 `teamId` 指向不存在的隊伍。
6. `status` 的值不在 `NONE`、`INVITED`、`JOINED` 之內。

受邀者的 email 一律轉為小寫，因為註冊時的連結與邀請時的查詢都以不分大小寫的方式比對，而唯一索引是逐位元組比對：留著大小寫不一致的舊資料，同一隊就可能存在兩筆只差大小寫的邀請，註冊時同時命中而寫入衝突。

以上檢查以**正規化後的結果**計算，而不是只看現況。擁有者數量以 `teams` collection 逐隊計算，沒有球員、或只有未連結球員的隊伍同樣會被檢查。檢查通過且寫入完成後，修正 `(teamId, email)` 索引、移除一般的 `(teamId, userId)` 索引、建立部分唯一索引，並以 `listIndexes` 驗證 `unique` 與 filter 確實存在。

audit 另外列出被撤回的舊邀請與帶 email 的未連結球員，以及正規化前帶擁有者角色的非成員，附角色與最後更新時間，讓開發者在執行前檢視。部分唯一索引沿用 MongoDB 的預設索引名稱，並在部署後的 audit 再以 `listIndexes` 確認一次，因為舊程式碼的 instance 可能依舊宣告重建索引。

### 第二階段：部署後連結

前提：本 Change 已部署到該環境，且在部署後**重跑正規化**、audit 各類歸零。正規化完成到部署生效之間，舊程式碼的日常操作（建立球員、離開隊伍、拒絕或取消邀請、變更角色）仍會寫出帶角色的未連結球員，這些文件在新程式碼下讀取失敗；部署切換期間舊的 instance 也還在寫入。部署後重跑正規化是連結的 gate。

連結的對象是狀態為 `INVITED` 且只有 email 的邀請：email 以與邀請時相同的方式（collation `strength: 2` 精確比對）恰好對應一位已註冊的使用者時，寫入 `userId` 並清除 `email`。

- **每一筆以條件更新寫入**：filter 同時比對 `_id`、`status: INVITED`、讀取時的 `email` 原值與「`userId` 不存在」。讀取之後管理員若取消邀請或改邀他人，這一筆不會被寫入，`matchedCount` 為 0 時跳過並列出。
- **逐筆處理錯誤**：唯一索引違反逐筆捕捉並列出，一筆失敗不中斷其餘寫入。
- **不寫入並列出**：該使用者在同隊已有球員、或 email 對應到多位使用者。

這一階段同時送達「已註冊卻收不到」的既有邀請。

### 執行順序

1. volleybro-test：執行正規化的 audit 與腳本，再跑一次 audit 確認歸零。
2. production：開發者執行正規化的 audit 與腳本，再跑一次 audit 確認歸零。
3. 部署本 Change 到 volleybro-test；重跑正規化直到 audit 歸零；執行連結的 audit 與腳本。
4. 部署本 Change 到 production。
5. production：開發者重跑正規化直到 audit 歸零，再執行連結的 audit 與腳本。

步驟 1、2 提前做，對權限是安全的：正規化只清除欄位、補上已成立的成員狀態，不增加任何人的權限。但它不能取代部署後的重跑（步驟 3、5），因為空窗期的舊寫入會再產生不符合模型的文件。在步驟 5 的重跑完成之前，這些文件所在的隊伍名單會讀取失敗，所以步驟 4 之後應立即執行步驟 5。連結不能提前到部署之前。

## Behavior contract

| 呼叫者與該隊的關係 | 成員檢查          | 管理員檢查          | 隊伍資訊、名單、單一球員 | 邀請清單中的隊名 |
| ------------------ | ----------------- | ------------------- | ------------------------ | ---------------- |
| 擁有者             | 通過              | 通過                | 可讀                     | —                |
| 管理員             | 通過              | 通過                | 可讀                     | —                |
| 一般成員           | 通過              | `INSUFFICIENT_ROLE` | 可讀                     | —                |
| 受邀者（任何角色） | `NOT_TEAM_MEMBER` | `NOT_TEAM_MEMBER`   | 不可讀                   | 可讀             |
| 登入但與該隊無關   | `NOT_TEAM_MEMBER` | `NOT_TEAM_MEMBER`   | 不可讀                   | —                |
| 未登入             | 401               | 401                 | 不可讀                   | —                |

- 受邀者接受邀請：`AcceptInvitationUseCase` 自己驗證受邀者身分，不經過成員檢查；接受後立即成為成員。
- 移轉擁有權：現任擁有者必須是角色為擁有者的成員，目標必須是成員；受邀者作為目標得到 `TARGET_NOT_MEMBER`。
- 被刪除的成員：`activeTeamId` 若指向該隊即清除；非成員直接開啟隊伍頁面時看到錯誤狀態。
- 不存在的隊伍：先授權再讀取，回傳 `NOT_TEAM_MEMBER`，不透露隊伍是否存在。`GetPlayerUseCase` 必須先讀出球員才知道隊伍，球員不存在時的行為維持不變：use case 回傳 null，route 交給回應 schema，呼叫者得到 400 VALIDATION。那個回應形狀是既有行為，本 Change 不動它。

## Failure modes

- **部署早於正規化**：缺 `status` 的文件在 `toPlayer` 縮窄時拋錯，而一筆壞文件就讓整隊名單讀不出來。以執行順序保證；S01 的驗收包含 production 正規化完成。
- **空窗期的舊寫入**：正規化完成到部署生效之間，舊程式碼仍會寫出帶角色的未連結球員。部署後立即重跑正規化，audit 歸零才執行連結。
- **連結時邀請已被取消或改邀他人**：讀取與寫入之間狀態改變，若無條件寫入，會把舊 email 對應的使用者寫進已取消或改邀的球員。以條件更新保證，未比對到的一筆跳過並列出。
- **email 比對連到別人的帳號**：以 `$regex` 做不分大小寫比對時 `.` 匹配任意字元。以 collation 精確比對保證，對應多位使用者時不寫入。
- **連結早於部署**：舊的權限檢查只看 `role`，被連結帳號的受邀者會在空窗取得成員與管理員權限。連結腳本屬於部署後的步驟，S03 的驗收寫明執行時機。
- **正規化時發現重複或不變量不成立**：腳本在寫入前停止。由開發者判斷保留哪一位球員、誰是擁有者，處理後重跑。
- **索引看似存在其實無效**：同名的一般索引或無效宣告會讓「索引已存在」的判斷誤以為完成。以 `listIndexes` 驗證 `unique` 與 filter。
- **邀請頁在隊伍端點收緊後失去隊名**：以切片順序保證，受邀者清單先帶上 `teamName`。
- **非成員開啟隊伍頁面時崩潰**：`team/info/index.tsx` 以 `team!.nickname`、`players!.length` 假設資料存在；讀取收緊後拋 TypeError。以切片順序保證，前端錯誤狀態先於讀取收緊。
- **接受邀請被新的成員檢查擋下**：`acceptInvitation` 的 controller 若仍經過 `GetPlayerUseCase`，受邀者會被自己的邀請擋下。同一個切片內改掉這條路徑。
- **既有測試以受邀者身分通過檢查、或期待未連結球員帶角色**：它們代表的正是被修掉的行為，應改寫而不是放寬判斷。
- **整合測試看不到授權**：`test/integration/support/auth.ts` 的 double 一律放行，拒絕的證據來自 entity 與 use case 的單元測試。

<RiskTable
  risks={[
    {
      name: "部署早於 production 正規化，隊伍名單讀取失敗",
      severity: "critical",
      mitigation:
        "執行順序寫入 Design 與 S01 的驗收；正規化不增加權限，可以提前完成。",
    },
    {
      name: "連結帳號早於部署，受邀者在空窗取得權限",
      severity: "critical",
      mitigation: "連結是部署後的獨立階段，腳本與驗收都寫明執行時機。",
    },
    {
      name: "空窗期的舊寫入讓部署後的隊伍名單讀取失敗",
      severity: "critical",
      mitigation:
        "部署後立即重跑正規化並確認 audit 歸零，作為連結的 gate；執行順序寫入 Design 與 S03 的驗收。",
    },
    {
      name: "連結腳本把邀請寫給已取消或改邀的球員，或連到別人的帳號",
      severity: "critical",
      mitigation:
        "條件更新比對讀取時的 status 與 email 原值；email 以 collation 精確比對，對應多位使用者時不寫入。",
    },
    {
      name: "正規化時發現重複或擁有者數量不為 1",
      severity: "warning",
      mitigation:
        "腳本在寫入前停止並列出文件，由開發者處理；不自動決定保留哪一位或誰是擁有者。",
    },
    {
      name: "邀請真的送達後，可以被用來把自訂隊名推到任意已註冊使用者面前",
      severity: "warning",
      mitigation:
        "本 Change 接受此風險：受邀者可以拒絕。使用者搜尋與邀請的 rate limit、封鎖與檢舉邀請另案處理。",
    },
    {
      name: "受邀者失去原本（錯誤地）擁有的權限",
      severity: "warning",
      mitigation:
        "這是本 Change 的目的。受影響的只有尚未接受邀請的使用者，接受後權限立即恢復。Changeset 以 Security 標示。",
    },
    {
      name: "缺 status 的舊邀請與帶 email 的未連結球員被撤回",
      severity: "info",
      mitigation:
        "D4 的刻意結果；audit 在執行前列出這些文件，需要時由管理員重新邀請。",
    },
    {
      name: "管理員失去降級或刪除擁有者、以及刪除自己的能力",
      severity: "info",
      mitigation: "D3 的刻意結果；擁有權只能由擁有者移轉，離隊走離開隊伍。",
    },
    {
      name: "GET /api/users/{userId}/players 為了隊名逐筆查詢隊伍",
      severity: "info",
      mitigation:
        "查詢數等於該使用者的隊伍與邀請數，量級很小。隊伍數成長到有感時再加批次查詢。",
    },
  ]}
/>

## Testing strategy

- **Entity**：`isTeamMember`、`hasTeamRole` 與角色管理判斷以狀態、呼叫者角色、目標角色、是否為自己的完整組合測試，不需要 mock。這是成員與管理規則唯一的權威。
- **Repository**：`toPlayer` 對三種合法文件縮窄成功，對缺 `status`、帶角色的未連結球員、同時有 `userId` 與 `email` 的受邀者拋錯。
- **Use case**：三個讀取 use case 斷言以正確參數呼叫檢查，且拒絕時不讀取 repository。`update-role` 與 `remove-player` 覆蓋 D3 的表格與自己為目標。`create-invitation` 覆蓋已註冊、未註冊、大小寫不同、`a.b@x.com` 不連到 `axb@x.com`、大小寫不同的兩位使用者回傳衝突、已在名單上、未註冊 email 重複邀請，並斷言非管理員時不查詢 email 也不揭露邀請狀態。`transfer-ownership` 以受邀者為目標斷言 `TARGET_NOT_MEMBER`。
- **Migration**：兩個階段的 audit 與腳本以記憶體 MongoDB 的整合測試覆蓋：每一類正規化、以正規化後結果計算的重複、每一項停止條件、既有同名一般索引與無效宣告下仍建立真正的唯一索引、重跑不改變結果、連結時遇到已在隊上的使用者不寫入；讀取後邀請被取消或改邀他人時不寫入；正規化後以舊寫入路徑產生的文件讓讀取失敗、重跑正規化後恢復；`null` email、成員缺 `userId`、未連結球員帶 `userId` 各自命中對應的規則或停止條件。
- **Component**：邀請清單以 `teamName` 顯示隊名且不呼叫 `useTeam`；隊伍資訊頁在讀取 403 時顯示錯誤狀態；建立球員表單只在填寫 email 時顯示角色；角色標籤顯示擁有者、管理員、一般成員。
- **Integration**：`pnpm test:integration` 作為接線未壞的證據。

## Decisions

<DecisionTimeline
  decisions={[
    aMemberHasJoined,
    teamReadsAreMembersOnly,
    adminsManagePeersOwnerIsProtected,
    backfillBeforeTheModelTightens,
  ]}
/>

## Revisit triggers

- 隱私設定上線，隊伍管理者可以把隊伍資訊或名單開放給非成員；屆時 D2 的端點授權改由可見性判斷決定。
- 會籍區塊要求 email 只給擁有者與管理員；屆時名單與單一球員的回應依角色裁切欄位。
- 擁有者移轉改為需要對方接受；屆時 D3 的擁有者保護與移轉流程一起重新檢視。
- 出現一位使用者在同一隊需要多個身分，或角色不再是線性階層。
