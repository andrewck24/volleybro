---
title: Design
---

import { DecisionTimeline } from "@/components/DecisionTimeline";
import { InteractiveFlowchart } from "@/components/InteractiveFlowchart";
import { RiskTable } from "@/components/RiskTable";
import { TLDR } from "@/components/TLDR";
import detailNeverCrossesTheWire from "./design/decisions/D1-detail-never-crosses-the-wire.json";
import oneCatalogueOneResolver from "./design/decisions/D2-one-catalogue-one-resolver.json";
import detailsCarriesPathOnly from "./design/decisions/D3-details-carries-path-only.json";
import constraintsPromptWhereKnowable from "./design/decisions/D4-constraints-prompt-where-they-are-knowable.json";

<TLDR>
  一個錯誤只被問兩次。伺服器問「這是什麼」，答案是 `reason` ——
  一個穩定的常數，沒有措辭。客戶端問「這該說什麼」，答案來自單一目錄，查不到就
  是通用訊息。散文從來沒有機會上路，所以也沒有機會被顯示。
</TLDR>

## Adopted shape

一則錯誤的完整路徑。點任一節點看它在本 Change 之後負責什麼。

<InteractiveFlowchart
  nodes={[
    { id: "throw", label: "throw AppError", x: 140, y: 50, w: 210 },
    { id: "log", label: "logError", x: 510, y: 50, w: 170 },
    { id: "serialize", label: "serializeError", x: 140, y: 150, w: 210 },
    { id: "proxy", label: "proxy.ts Edge 401", x: 420, y: 150, w: 200 },
    {
      id: "body",
      label: "code + reason + status",
      x: 280,
      y: 250,
      w: 260,
    },
    { id: "parse", label: "parseApiError", x: 280, y: 350, w: 210 },
    {
      id: "resolve",
      label: "resolveErrorDisplay",
      x: 280,
      y: 450,
      w: 230,
    },
    { id: "catalogue", label: "ERROR_MESSAGES", x: 590, y: 450, w: 200 },
    { id: "unknown", label: "UNKNOWN 條目", x: 590, y: 565, w: 200, h: 44 },
    { id: "toast", label: "toast", x: 160, y: 560, w: 150 },
    { id: "dialog", label: "AlertDialog", x: 370, y: 560, w: 170 },
  ]}
  edges={[
    { from: "throw", to: "log", dashed: true, label: "只進 log" },
    { from: "throw", to: "serialize" },
    { from: "serialize", to: "body" },
    { from: "proxy", to: "body" },
    { from: "body", to: "parse" },
    { from: "parse", to: "resolve" },
    { from: "resolve", to: "catalogue", label: "key" },
    { from: "catalogue", to: "unknown", dashed: true, label: "查不到" },
    { from: "resolve", to: "toast" },
    { from: "resolve", to: "dialog" },
  ]}
  details={{
    throw: {
      title: "throw AppError(reason, detail, internalMessage?)",
      body: "三格維持不變。改變的是誰讀得到哪一格：reason 上 wire，detail 與 internalMessage 只進 log。detail 留在型別上，因為它仍是 Error.message 的來源。",
    },
    log: {
      title: "logError",
      body: "記錄 code、reason、message 與（非預期錯誤的）stack。message 是 internalMessage ?? detail，所以帶識別碼的那一句仍然完整進 log。本 Change 對伺服器端 log 沒有任何影響。",
    },
    serialize: {
      title: "serializeError",
      body: "停止寫入 detail。同檔案那個標為 unreachable 的 fallback body 也寫了一份 detail，一併移除——它確實到不了，但它讓 detail 繼續存在於 wire 契約上。",
    },
    proxy: {
      title: "proxy.ts 的 Edge 401",
      body: "第二個回應產生點，完全不經過 serializeError。它今天送出的 detail 是「Authentication is required」，而 Node 端同一個 reason 送的是「Authentication is required to access this resource」——同一件事兩種措辭。改成 reason-keyed 之後這個分歧消失，代價是 Edge runtime 也必須動。",
    },
    body: {
      title: "封閉的回應 body",
      body: "code、reason、status，加上驗證失敗時只含 path 的 details。這是一個封閉集合：新增自由文字欄位就是推翻 D1，不論它叫什麼名字。",
    },
    parse: {
      title: "parseApiError",
      body: "ApiError 介面拿掉 detail，連同「缺漏時合成一句」的預設值。型別不再承諾一個不會到達的欄位，tsc 因此能誠實列舉每一個讀取者。normalizeNetworkError 自造的 detail 同時移除。",
    },
    resolve: {
      title: "resolveErrorDisplay(error: unknown): ErrorMessage",
      body: "total function，沒有 null。參數是 unknown 因為 catch 抓到的就是 unknown——判斷型別是這個函式的責任，不是 16 個呼叫端各自的責任。401 解析成 SESSION_EXPIRED；全域處理器已經彈過內容相同的一則，而 toast 上限為 1、後者取代前者，看不出差別。",
    },
    catalogue: {
      title: "ERROR_MESSAGES",
      body: "使用者可見錯誤字串的唯一存放處。key 是 reason 常數或四個類別常數之一，value 一律是 { title, description }。類別常數以型別約束，打錯字是編譯錯誤而不是靜默降級。",
    },
    unknown: {
      title: "fail-closed 的預設",
      body: "查不到 key 就回這一則，永遠不讀伺服器欄位。這條分支在窮盡性測試上線後仍然可達：客戶端自己發明的 TIMEOUT 與 NETWORK_ERROR 不屬於任何 enum。",
    },
    toast: {
      title: "toast",
      body: "渲染 title 與 description。13 個呼叫點，其中 3 個其實是讀取路徑（全域 onError 與兩個下拉更新），不是 mutation。",
    },
    dialog: {
      title: "AlertDialog 內的 inline 錯誤",
      body: "3 個呼叫點，全部是不可逆的成員操作，失敗後 dialog 不關。渲染 description；dialog 自己的標題是為「意圖」寫的（確定要離開這個隊伍嗎），不是為錯誤寫的。要不要一併渲染 title 留到有真實文案時再看畫面決定。",
    },
  }}
/>

### 兩個問題，兩個擁有者

分層的判準只有一句：**這個問題的答案，誰知道得最早。**

| 問題                   | 誰回答     | 形式                             |
| ---------------------- | ---------- | -------------------------------- |
| 發生了什麼             | 伺服器     | `reason` 常數，沒有措辭          |
| 使用者該讀到什麼       | 客戶端     | `ERROR_MESSAGES[reason]`         |
| 要不要顯示、顯示在哪裡 | 呼叫點     | modal 開著就進 modal，否則 toast |
| 這個約束該在哪裡提示   | 可知性決定 | 客戶端判斷得出來的就不送出（D4） |

第三列不是一條需要文件承載的規則，而是第二條互動規則的推論：不可逆的動作會拿到確認 dialog，而失敗當下 dialog 還開著，錯誤就沒有別的地方可去。七個動作站點全部符合，沒有例外。

### 為什麼重複的 title 是刻意的

目錄裡大約二十個 4xx 條目會在一開始重複寫同一個標題。這看起來像是該被抽出去的重複，而它確實可以——代價是**先驗地規定 title 不隨 reason 變動**。

那是對今天貧乏文案的觀察，不是約束。`OWNER_CANNOT_LEAVE` 配「無法離開隊伍」明顯勝過通用的「操作失敗」，而改善文案正是本 Change 的目的之一。把結構鎖成兩張表會擋掉這個選項，也讓 i18n 有兩種形狀要遷移而不是一種。

**重複讓每個標題成為一個看得見、改得動的格子。** 抽出去會讓它變成一張表悄悄替所有 reason 決定的東西。

## Contracts

- **錯誤 body 是封閉集合。** `code`、`reason`、`status`，加上驗證失敗時只含 `path` 的 `details`。兩個產生點（`withErrorHandler` 與 `proxy.ts`）都遵守。
- **`reason` 是穩定的 wire 常數。** 它已經是 client 據以判斷的東西，本 Change 讓它成為唯一的判斷依據。新增 reason 而不新增文案，會讓窮盡性測試變紅。
- **`detail` 留在 `AppError` 上，但不上 wire。** 它仍是 `Error.message` 的來源，伺服器端 log 的內容完全不變。
- **`resolveErrorDisplay` 是 total function。** 沒有回傳 `null`、沒有 sentinel，呼叫端不需要分支。
- **文案只有一個來源。** `ERROR_MESSAGES` 之外不得出現使用者可見的錯誤字串——這是 i18n 遷移只需要動一個檔案的唯一條件。
- **`logError` 不變。** 它記的是 `code`、`reason`、`message`、`stack`，`detail` 從來不是它的欄位。

## Testing strategy

四層，各證明不同的事。

**wire 契約測試——證明散文真的不在 body 裡。** 兩個產生點各一組：`withErrorHandler` 的 4xx 與 5xx，以及 `proxy.ts` 的 Edge 401。斷言的形式是「body 的 key 集合等於預期集合」而不是「body 沒有 detail」——前者會擋下未來以別的名字加回來的自由文字，後者不會。

**窮盡性測試——證明每個 reason 都有文案。** 從 `src/entities/errors/index.ts` 的匯出推導，不得手寫清單。原始 issue 手寫了四個 enum、漏掉第五個，正是這種清單會漂移的證明。

**fail-closed 測試——證明查不到時退到安全值。** 這條分支在窮盡性測試上線後仍然可達，因為客戶端自己發明的 `TIMEOUT` 與 `NETWORK_ERROR` 不屬於任何 enum。

**顯示路徑測試——證明兩個 surface 對同一個錯誤說同一句話。** 同一個 `ApiClientError` 餵給 toast 與 dialog 兩條路徑，斷言 description 相同。這是今天最不成立的一件事，所以它需要一個直接的釘子。

既有的 12 個測試檔會紅。它們斷言的是 `detail` 的內容，更新方式是改成斷言 `reason`——**斷言可觀察的契約，不是斷言措辭**。

## Risks

<RiskTable
  risks={[
    {
      name: "proxy.ts 是第二個回應產生點，很容易漏",
      severity: "warning",
      mitigation:
        "它不經過 serializeError，所以任何以「改 serializeError」為心智模型的實作都會漏掉它，而漏掉的後果是 Edge 401 繼續送出散文、契約只成立一半。緩解是把它與 serializeError 綁在同一個切片，且 wire 契約測試對兩個產生點各有一組。",
    },
    {
      name: "拿掉 getter 但沒改建構子，detail 會活在 error.message 上",
      severity: "warning",
      mitigation:
        "ApiClientError 用 info.detail 當 Error 的 message。只拿掉 getter 的話 tsc 會宣告乾淨，而字串仍然可讀，且 .message 的讀取者不會被列舉。緩解是把建構子與 getter 放在同一個切片，並在該切片的驗收條件裡明列。",
    },
    {
      name: "reason 掛錯的地方會被新機制放大",
      severity: "warning",
      mitigation:
        "profile.usecase 把 protected-field 的錯誤配給 INVALID_EMAIL。今天無害因為沒人看 reason；改成 reason-keyed 之後使用者會被告知 email 格式錯誤。已知只有這一處，但它證明「reason 與訊息一致」從未被檢查過。緩解是它獨立成一個排在文案之前的切片，且盤點時逐一核對每個 reason 與它的訊息是否指同一件事。",
    },
    {
      name: "窮盡性測試可能退化成儀式",
      severity: "warning",
      mitigation:
        "它的通過條件是「每個 reason 都有條目」，塞佔位字串就能轉綠，而綠燈會讓人以為文案齊全。緩解是排序：可達性盤點先做，文案跟著盤點寫，窮盡性測試最後才上線——測試守的是「別再漏掉」，不是「現在有了」。",
    },
    {
      name: "TIMEOUT 與 NETWORK_ERROR 靠 status 而非 code 被攔下",
      severity: "info",
      mitigation:
        "這兩個由客戶端發明的 reason 不屬任何 enum，今天走不到 reason 查表，只因為它們的 status 是 503。它們的 code 是 TRANSIENT 而不是 UNEXPECTED，所以改 status 就會掉進查表。fail-closed 預設本來就接得住，記為 revisit trigger 而非阻礙。",
    },
    {
      name: "401 門閂靠整頁載入重置",
      severity: "info",
      mitigation:
        "swr-provider 的 redirecting 設了不重置，而它之所以還能運作，只因為登入唯一的路徑是 Google OAuth、必然整頁載入。本 Change 不修釋放條件（那是 session 生命週期的決定），只留一行註解記下這個相依。本 Change 移除 401 的提早 return，順帶讓第二次以後的 401 至少會顯示訊息。",
    },
    {
      name: "客戶端的 form schema 仍是手抄而非衍生",
      severity: "info",
      mitigation:
        "D4 明確把它留在原地，由既有的漂移測試守著。真正的解法是從 request schema 衍生，而那件工作被本 Change 卡著——D3 把英文訊息移出契約之後才能動。記在 D4 的替代方案裡，避免被誤讀成本 Change 的疏漏。",
    },
  ]}
/>

## Decisions

<DecisionTimeline
  decisions={[
    detailNeverCrossesTheWire,
    oneCatalogueOneResolver,
    detailsCarriesPathOnly,
    constraintsPromptWhereKnowable,
  ]}
/>

## References

- `request-schema-boundary`——本 Change 的來源。它建立了「邊界回答形狀、entity 回答意義」的分工，並在驗證過程中發現這裡的問題
- `WriteError`（`src/lib/features/game/types.ts`）——同一個決定的既有先例：只留 `code` / `reason` / `status`，理由已寫在該處註解
- `platform/clean-architecture`——「客戶端保有自己的 request-shaped schema」這條 Known gap，本 Change 補上對稱的另一半
- `rally-entry-validation` D2——「兩層驗證不應互相複製」的既有記載，D4 的階段判準與它同源
