---
title: Review
---

import { AnnotatedDiff } from "@/components/AnnotatedDiff";
import { FileTour } from "@/components/FileTour";
import { RiskTable } from "@/components/RiskTable";
import { TLDR } from "@/components/TLDR";

<TLDR>
  兩個前提在過程中被自己的查證推翻——一次在提案階段（這不是識別碼洩漏），一次在執行階段（空的對手名稱從來沒有被拒絕過）——兩次都改寫了工作本身，而不是只改措辭。
</TLDR>

## 交付內容

<FileTour
  files={[
    {
      path: "src/lib/api/error-messages.ts",
      change: "added",
      summary:
        "使用者可見錯誤字串的唯一存放處。25 個 reason 中的 21 個各有一則，其餘四則由類別常數吸收並以 ABSORBED_REASONS 記名。key 打錯是編譯錯誤。",
      lang: "ts",
      code: 'export const ERROR_MESSAGES: Record<CatalogueKey, ErrorMessage> = {\n  SESSION_EXPIRED: { title: "\u767b\u5165\u903e\u671f", description: "\u8acb\u91cd\u65b0\u767b\u5165" },\n  OWNER_CANNOT_LEAVE: {\n    title: "\u968a\u9577\u7121\u6cd5\u96e2\u958b\u968a\u4f0d",\n    description: "\u8acb\u5148\u628a\u968a\u9577\u8f49\u79fb\u7d66\u5176\u4ed6\u6210\u54e1\uff0c\u518d\u96e2\u958b",\n  },\n  // \u202620 \u500b reason \u689d\u76ee\n};',
    },
    {
      path: "src/lib/api/error-toast.ts",
      change: "modified",
      summary:
        "兩條互相矛盾的顯示路徑收斂成一個 total function。getErrorMessage 這個名字退役。401 的提早 return 移除。",
      lang: "ts",
      code: 'export function resolveErrorDisplay(error: unknown): ErrorMessage {\n  if (error instanceof RefreshTimeoutError) return ERROR_MESSAGES.NETWORK_TIMEOUT;\n  if (!(error instanceof ApiClientError)) return ERROR_MESSAGES.UNKNOWN;\n  if (error.status === 401) return ERROR_MESSAGES.SESSION_EXPIRED;\n  // \u5ba2\u6236\u7aef\u81ea\u9020\u7684 reason \u5fc5\u9808\u6392\u5728 5xx \u4e4b\u524d\uff0c\u5426\u5247\u6703\u88ab 503 \u541e\u6389\n  if (error.reason === "TIMEOUT" || error.reason === "NETWORK_ERROR")\n    return ERROR_MESSAGES.NETWORK_TIMEOUT;\n  if (error.status >= 500 || error.code === "UNEXPECTED")\n    return ERROR_MESSAGES.SERVER_ERROR;\n  const entries: Record<string, ErrorMessage | undefined> = ERROR_MESSAGES;\n  return entries[error.reason] ?? ERROR_MESSAGES.UNKNOWN;\n}',
    },
    {
      path: "src/components/custom/error/error-notice.tsx",
      change: "added",
      summary:
        "dialog 的錯誤欄位改存整個 ErrorMessage 而不是它的 description，三個站點共用這一個 notice 把兩半都渲染出來。",
      lang: "tsx",
      code: 'export const ErrorNotice = ({ message }: { message: ErrorMessage }) => (\n  <AlertDialogBody className="gap-1 text-sm text-destructive">\n    <p className="font-medium">{message.title}</p>\n    <p>{message.description}</p>\n  </AlertDialogBody>\n);',
    },
    {
      path: "src/lib/api/wrappers.ts",
      change: "modified",
      summary:
        "serializeError 與那個 unreachable fallback 都收斂為 code + reason；驗證失敗的 details 縮成欄位路徑。",
      lang: "ts",
      code: "const body: Record<string, unknown> = {\n  code: error.code,\n  reason: error.reason,\n  detail: error.detail, // [!code --]\n};\nif (error instanceof ValidationError && error.details !== undefined) {\n  body.details = error.details; // [!code --]\n  body.details = error.details.map(toFieldPath); // [!code ++]\n}",
    },
    {
      path: "src/proxy.ts",
      change: "modified",
      summary:
        "Edge 401 是第二個回應產生點，完全不經過 serializeError。只改 Node 那條會讓契約只成立一半。",
      lang: "ts",
      code: 'return NextResponse.json(\n  { code: "AUTHENTICATION", reason: AuthReason.SESSION_REQUIRED },\n  { status: 401 },\n);',
    },
    {
      path: "src/lib/api/api-client.ts",
      change: "modified",
      summary:
        "拿掉 .detail getter，並停止把 detail 當 Error 的 message——少了後者，字串會活在 .message 上而 tsc 什麼都不會列舉。",
      lang: "ts",
      code: "throw new ApiClientError(info.detail, info); // [!code --]\nthrow new ApiClientError(info.reason, info); // [!code ++]",
    },
    {
      path: "src/applications/usecases/user/profile.usecase.ts",
      change: "modified",
      summary:
        "刪除一個三重不可達的 guard：型別已排除該欄位、route 的 .strict() 擋下、兩個呼叫端都不傳。",
      lang: "ts",
      code: 'if ("userId" in updates || "id" in updates) { // [!code --]\n  throw new ValidationError( // [!code --]\n    ProfileReason.INVALID_EMAIL, // [!code --]\n    "Cannot update userId or id fields", // [!code --]\n  ); // [!code --]\n} // [!code --]',
    },
    {
      path: "src/lib/features/game/types.ts",
      change: "modified",
      summary:
        "兩處比 request schema 寬鬆的欄位收緊到控制項已經強制的範圍。使用者能做的事沒有改變。",
      lang: "ts",
      code: 'teams: z.object({\n  home: z.object({ name: z.string().optional() }), // [!code --]\n  home: z.object({ name: z.string() }), // [!code ++]\n}),\nscoring: z.object({\n  setCount: z.string(), // [!code --]\n  setCount: z.enum(["1", "3", "5"]), // [!code ++]\n}),',
    },
    {
      path: "jest.config.ts",
      change: "modified",
      summary:
        "backend project 納入 src/__tests__/**。在此之前 proxy.test.ts 不符合任何 testMatch，從未執行過。",
      lang: "ts",
      code: 'testMatch: [\n  "<rootDir>/src/app/apple-splash/**/*.{spec,test}.{js,jsx,ts,tsx}",\n  "<rootDir>/src/__tests__/**/*.{spec,test}.{js,jsx,ts,tsx}", // [!code ++]\n],',
    },
  ]}
/>

## 兩個被推翻的前提

計畫階段與執行階段各一次。兩次都是查證推翻了工作自己寫下的敘述。

### 一、這不是識別碼洩漏

原始 issue 說 `Player 6721a not found in team abc` 已經在 response body 裡。**不是。** `AppError` 有三格，那個字串在第三格，只進 log，而且早就有一個測試釘住它不外洩。全 repo 113 個建構點裡只有兩處內插真實 id，兩處都正確地在第三格。

真正成立的是**內部詞彙**上了 wire，而且它是被 `docs/specs/error-handling/spec.md` **明文規定**的行為——那份 spec 說 `detail` 是「safe for HTTP responses」，並且規定查不到對照時 `SHALL display error.detail`。所以這個 Change 推翻的是四條寫下來的 requirement，不是一個沒人執行的慣例。

### 二、空的對手名稱從來沒有被拒絕過

S07 原本的驗收寫著「今天它會繞一趟網路回來給一個通用失敗訊息」。用真實 payload 走真實的 JSON 序列化實測：

<AnnotatedDiff
  lang="ts"
  code={
    'const wire = JSON.parse(JSON.stringify(newGameBody({ info, teamId, players })));\nCreateGameSchema.safeParse(wire);\n// away.name sent: ""\n// success: true   \u2190 request schema \u7684 z.string() \u63a5\u53d7\u7a7a\u5b57\u4e32'
  }
/>

沒有任何一層擋它，而每一個渲染隊名的地方都已經有 `對手` 的 fallback。**沒有錯置的約束——那個約束根本不存在。** 開發者確認空名稱本來就該被接受，S07 因此改寫為「form 不再接受 server 會拒絕的東西」，範圍縮到兩處控制項已經強制的收緊。

第一次實測我把物件直接餵給 `safeParse`，`Date` 沒經過序列化，得到一個假的型別錯誤，差點回報一個不存在的缺陷。

## Review findings

兩軸各自在獨立 context 執行。

<RiskTable
  risks={[
    {
      name: "Spec：窮盡性測試手寫五個 enum 名稱",
      severity: "warning",
      mitigation:
        "接受並已修。這正是該測試自己的驗收條件禁止的事，也正是 ProfileReason 第一次被漏掉的方式——而那個手寫指示是我下給執行者的。改為從模組匯出推導，並以新增第六個 enum 實測會紅來驗證；舊版本會通過。",
    },
    {
      name: "Spec：一個 dialog 測試丟出不存在的 reason",
      severity: "warning",
      mitigation:
        "接受並已修。它丟 NOT_ALLOWED（任何 enum 都沒有），因此斷言的是通用 fallback，對它命名的那條文案路徑什麼都沒證明。改成丟該 dialog 真實會產生的 reason 並斷言那則文案。",
    },
    {
      name: "Standards：401 門閂註解重述 Blueprint 已擁有的推理",
      severity: "warning",
      mitigation:
        "接受並已修。CONTRIBUTING 明文要求引用 Change 而非複製，且一到兩行為常態；原本是六行。壓成兩行加 slug-qualified 引用。",
    },
    {
      name: "Standards：目錄的 key 手抄 enum 成員",
      severity: "warning",
      mitigation:
        "接受，兩輪才修完。第一輪只把 ABSORBED_REASONS 改成 enum 成員，ReasonKey 仍手抄 20 個字面量——同一個檔案兩種寫法，而本頁一度把它記成已修。第二輪把 ReasonKey 改為 Exclude<所有 reason, 被吸收者>，重新命名一個 reason 現在會產生 9 個編譯錯誤。",
    },
    {
      name: "Standards：changeset 未提 409 → 404",
      severity: "info",
      mitigation:
        "駁回，附證據。全 repo 沒有任何 client 邏輯依 409/404 分支；取消邀請的 catch 只呼叫 showErrorToast，依 reason 解析出同一則文案。使用者觀察不到差別，屬 changeset 規則明列應省略的「無行為變更的內部重構」。",
    },
    {
      name: "Spec round 2：reason token 被渲染給使用者",
      severity: "warning",
      mitigation:
        "接受並已修，這是本 Change 自己造成的退化。S02 把 ApiClientError 的 message 從 detail 改成 reason，而 team/form.tsx 直接把 e.message 放進表單錯誤，於是使用者會讀到 DUPLICATE_RESOURCE 這種裸 token。D1 的風險表預告過這條，我卻只掃了 .detail 的讀取者，沒掃 .message——而 .message 正是 tsc 不會列舉的那一個。三個表單改走 resolver。",
    },
    {
      name: "Spec round 3：第四份複製，一字之差",
      severity: "warning",
      mitigation:
        "接受並已修，而且改變了處理方式。unconfirmed-set-dialog 以一個 const 持有 哎呀，發球掛網了！——與目錄的 哎呀，發球掛網！ 差一個字，註解還指向一個早已不含那句話的檔案。三次人工掃描各自漏掉不同形狀的複製（物件欄位、其他形狀、裸 const），所以第四次的回應不是再掃一次，而是一個測試：目錄的每一則字串在 src/ 底下只能出現在目錄自己的檔案裡。以植入一份複製實測會紅。它抓不到已經漂移的複製，這一點寫在測試自己的註解裡。",
    },
    {
      name: "Spec round 2：目錄不是唯一存放處",
      severity: "warning",
      mitigation:
        "接受並已修。第一輪的 sweep 以 title:/description: 為條件，漏掉三處不同形狀的字串：兩個表單的 root error，以及 ServerErrorState——後者由 7 個讀取失敗分支渲染，文案已經與目錄的 SERVER_ERROR 分歧，正是單一目錄要防的漂移。三處都改為由目錄取得。",
    },
    {
      name: "Standards round 2：註解在縮短時被切壞",
      severity: "warning",
      mitigation:
        "接受並已修。第一輪為了符合長度規則而縮短 401 門閂的註解，腳本式編輯留下前一句的殘骸接上新句，成為不成句的兩行。改寫成一句完整的話。同一輪也移除了兩個重述函式簽章的 doc comment。",
    },
    {
      name: "Standards round 2：兩個 sibling 測試用不同手法證明同一件事",
      severity: "info",
      mitigation:
        "接受並已修。alert-dialog-error-state 仍以 partial mock 加 spy 驗證行為，而 testing-strategy 規定 Component 層只 mock API 與 custom hooks 且採狀態驗證。收斂成 sibling 的做法：不 mock 顯示模組，斷言渲染結果。",
    },
    {
      name: "Spec：驗證回應未以 key 集合斷言",
      severity: "info",
      mitigation:
        "接受並已修。那是最可能長出新欄位的 body，現在與另外兩個產生點一樣以 key 集合斷言。",
    },
  ]}
/>

### 最尖銳的一則

窮盡性測試曾經是這樣：

<AnnotatedDiff
  lang="ts"
  code={
    'import { AuthReason, CommonReason, GameReason, PlayerReason, ProfileReason } from "@/entities/errors"; // [!code --]\nimport * as errors from "@/entities/errors"; // [!code ++]\n\nconst allReasons = [ // [!code --]\n...Object.values(AuthReason), ...Object.values(CommonReason), // [!code --]\n...Object.values(GameReason), ...Object.values(PlayerReason), // [!code --]\n...Object.values(ProfileReason), // [!code --]\n]; // [!code --]\nconst allReasons = Object.values(errors) // [!code ++]\n.filter(isReasonEnum) // [!code ++]\n.flatMap((reasonEnum) => Object.values(reasonEnum)); // [!code ++]'
  }
/>

舊版本會漏掉第六個 enum——正是它存在的理由所描述的失敗。實測：新增一個 enum 並匯出後，新版本紅、舊版本綠。

## 計畫與實際的差異

- **文案盤點提前到核准之前。** 計畫原本把它放在執行階段。提前之後當場找到兩個 reason 語意錯誤，兩者都會以錯誤文案出貨。
- **S04 從「改標籤」變成「刪除」。** 那個 guard 三重不可達，不需要新的 reason 常數。
- **S05 沒有拆成 5a/5b。** 盤點在核准前完成之後，5a 沒有工作可做。
- **`jest.config.ts` 不在任何切片的計畫內。** 寫 S01 的測試時才發現 `proxy.test.ts` 從未執行過。
- **兩件事在開發者驗收時才定案。** AlertDialog 改為同時渲染 title 與 description；盤點找到的兩個 reason 語意缺陷由新的 `GameReason.STALE_LINEUP` 接手。兩者都改了程式，也都回寫進 D2、S03 與文案盤點。
- **驗收時的 verifier 抓到一個既有缺陷，把新文案架空了一半。** 換人面板的 `mutate(...)` 沒有 `await`，未處理的 promise 拒絕不會進外層 `catch`，所以換人失敗時樂觀更新默默回滾、使用者什麼都看不到——`STALE_LINEUP` 的第一個拋出點正好走這條路徑。修法是啟動寫入、照舊立刻 dispatch 草稿確認、然後 `await`，時序不變而拒絕有人接。同型的其他呼叫點掃過了：其餘 `mutate` 不是傳純資料就是走 outbox，沒有第二處。

## 殘留風險與後續

<RiskTable
  risks={[
    {
      name: "PLAYER_NOT_FOUND 仍扛著「Player has no team」",
      severity: "info",
      mitigation:
        "驗收時裁決為不修。它是 player 記錄沒有 teamId 的資料不變式破損，補救動作是找我們而非使用者能做的事；給它專屬文案等於把系統的問題寫成使用者的問題。同輪找到的另外兩個語意缺陷已由新的 GameReason.STALE_LINEUP 接手，見 Design 的〈盤點找到的 reason 語意缺陷〉。",
    },
    {
      name: "game 的兩個表單仍手抄 request schema",
      severity: "info",
      mitigation:
        "球員表單已經直接用 request schema 當 resolver——想要的形狀在這個 repo 已經存在。game 的兩個是僅存的複製品，由既有的漂移測試雙向守著，其消除屬於 schema 衍生那件工作。",
    },
    {
      name: "目錄住在 src/lib/api/，而它沒有 API 關注點",
      severity: "info",
      mitigation:
        "Standards round 3 的觀察：ServerErrorState 這類 presentational component 匯入 @/lib/api/ 讀起來像跨層，同性質的 ROLE_LABELS 住在 lib/constants/。不現在搬——lib/api/ 其餘檔案的歸屬被三張未定案的票綁住，現在決定等於猜。已記在分層稽核那張票的 revisit trigger 裡。",
    },
    {
      name: "Archive 需要一個尚未存在的 capability 目錄",
      severity: "info",
      mitigation:
        "四則 ADR 指向 platform/error-handling。該 capability 已被 Features IA 決定存在、兩份 spec 都在、platform 首頁也已在宣傳它，但 Feature 頁從未被建出來。本 Change 是第一個需要它的工作。",
    },
  ]}
/>
