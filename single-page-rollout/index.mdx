---
title: Single Page Rollout
description: 七個兩頁格式的 Change 改成單頁，刪除兩頁路由；publish 替已合併的 Change 取落地範圍的數字與 mergedAt；store 分支有 push 就觸發正式站部署。
capabilities: ["platform/blueprint"]
---

export const scenarios = [
  {
    id: "S1",
    given: "the seven two-page Changes have been migrated",
    when: "a reader opens `/changes/<slug>` for any of them, or an old `/changes/<slug>/proposal` or `/review` URL",
    then: "the Change renders as one page with a Proposal tab and a Review tab, and the old URLs return 404",
  },
  {
    id: "S2",
    given: "a migrated Change's Proposal tab",
    when: "a reader opens it",
    then: "its decisions render as decision cards and its acceptance scenarios as scenario cards, and `two-gate-workflow` cites ADR-0045 instead of an in-page decision",
  },
  {
    id: "S3",
    given: "a migrated Change's Review tab",
    when: "a reader opens it",
    then: "where the old page had an acceptance-result table it shows `ScenarioResults` cards, and every other section keeps its original text",
  },
  {
    id: "S4",
    given: "a Change that has already merged into dev",
    when: "its page is published",
    then: "facts.json takes its figures and `mergedAt` from the commit that landed it, and a squash-merged Change records `commits` as null",
  },
  {
    id: "S5",
    given: "a Change that has not merged yet",
    when: "its page is published",
    then: "facts.json takes its figures from the branch diff against dev, and `mergedAt` is null",
  },
  {
    id: "S6",
    given: "the deploy-hook workflow",
    when: "the blueprint-changes branch receives a push",
    then: "it calls the production deploy hook, and fails when the hook secret is missing",
  },
  {
    id: "S7",
    given: "the two-page route has been removed",
    when: "Blueprint builds",
    then: "the build passes and the old-format Changes still render",
  },
  {
    id: "S8",
    given: "a migrated Change page",
    when: "the developer opens it in a desktop browser and on a phone",
    then: "every section reads correctly",
  },
];

<ChangeTabs>
<Proposal>

<TLDR>
單頁格式已經交付，但兩頁格式的 Change 仍走舊路由，決策與驗收還是舊的表格和時間軸。publish 產生的數字一律取自當下分支，對已合併的 Change 重新發佈就會寫錯。另外，store 分支的新頁面要等 `dev` 下次 push 才會上正式站。這個 Change 把這些 Change 全部改成單頁、刪除兩頁路由；已合併的 Change 改以它落地的 commit 計算數字，並記錄 `mergedAt`；store 分支一有 push 就觸發正式站部署。
</TLDR>

| | 現在 | 之後 |
| --- | --- | --- |
| 兩頁格式的 Change | `/proposal`、`/review` 兩個網址，決策用時間軸、驗收用表格 | 一頁兩個 tab，決策卡片、scenario 卡片、`ScenarioResults` 卡片 |
| 重新發佈已合併的 Change | 數字取自當下分支，是錯的 | 取自它在 `dev` 落地的 commit，並寫入 `mergedAt` |
| 新發佈的頁面上正式站 | 等 `dev` 下次 push | store 分支 push 後立即觸發部署 |

本 Change 沒有新的決策紀錄：它落實的是 ADR-0072（一個 Change 一頁）與 ADR-0074（數字來自產生的 facts）。

## 範圍

1. **遷移 7 個兩頁 Change，只動結構**：`two-gate-workflow`、`standalone-decision-records`、`reviewer-standards-split`、`commit-guardrails`、`opaque-status-bar`、`triage-state-mapping`、`single-page-change`。
   - Proposal：`DecisionTimeline` 改成 `DecisionCards`，`<Scenario>` 改成 `scenarios` export；`two-gate-workflow` 的頁內決策改為引用 ADR-0045。
   - Review：驗收結果表改成 `ScenarioResults` 卡片，code review 與殘留風險包進 `ReviewDetails`，其餘段落保留原文。
   - 遷移過來的頁面不跑 G2 必要區塊檢查，這條寫進 `docs/agents/blueprint.md`。
2. **刪除兩頁格式的程式碼**：route 的 `/proposal`、`/review` 分支、`ChangeDecisionTimeline`、`proposalMockups`。不做轉址。
3. **publish 的 `facts.json`**：已合併的 Change 以落地的 commit 計算。merge commit 取兩個 parent 之間的範圍；squash commit 取它自己的 diff，`commits` 記為 `null`。新增 `mergedAt`，記錄落地 commit 的時間；還沒合併的照舊使用分支 diff，`mergedAt` 為 `null`。
4. **deploy hook**：GitHub Action 在 `blueprint-changes` 有 push 時呼叫 `CLOUDFLARE_BLUEPRINT_DEPLOY_HOOK`，secret 不存在就失敗。`dev` 仍由 Cloudflare 自行建置，重複建置先不處理。

之後：`/changes` 改回卡片、依 `mergedAt ?? publishedAt` 由新到舊排序，並建立 Blueprint 首頁，另外做成一個 Change。

<Scenarios items={scenarios} />

<RiskTable
  risks={[
    { name: "遷移時改到已驗收的內容", severity: "warning", mitigation: "每頁遷移前後都去掉空白與元件標籤後比對文字。" },
    { name: "刪除路由弄壞舊格式頁面", severity: "warning", mitigation: "S7 與 Blueprint 建置。" },
    { name: "找錯某個 Change 的落地 commit", severity: "info", mitigation: "只認 `dev` first-parent 歷史中分支名或 `Blueprint-Change` trailer 與 slug 完全相符的 commit；找不到就照未合併處理。" },
  ]}
/>

</Proposal>
<Review>

<ActionItems>

1. 接受下方「與 Proposal 的偏差」五項（前四項已接受；第 5 項是依你在 G2 的回饋所做的修改）。

</ActionItems>

<ReviewFocus>

1. `scripts/change-page.js` 的 `landingOf`：如何在 `dev` 的 first-parent 歷史中找到一個 Change 落地的 commit。merge commit 看 subject 裡的分支名稱，squash commit 看 `Blueprint-Change` trailer；找不到就當作還沒合併。
2. `scripts/blueprint-changes.js` 的 `syncDeployWorkflow` 與 pull 的過濾：deploy workflow 為什麼要放進 orphan 的 store 分支，以及 pull 為什麼要跳過它。
3. 7 個遷移後的頁面：挑一頁比對 Proposal 與 Review 兩個 tab，確認原文都還在。

</ReviewFocus>

<Deviations>

1. **`ScenarioResults` 多了「未完整驗證」這個結果（`unverified`）。** 三個舊 Change 的驗收表寫著「未證明」、「部分證明」、「由程式碼保證」。把這些寫成 pass 會誇大，寫成 pending 則不符實情，所以新增這個值，原本的措辭保留在證據欄。gate 只把 pass／fail 算成結果，所以新頁面不能拿它來充數。
2. **修正 `/changes` 索引。** 索引原本只列出有 `/proposal` 或 `/review` 子頁的 Change，單頁格式從來沒被列進去。不修的話，這 7 頁遷移後就會從索引消失。
3. **頁首單複數。** 頁首把「1 commits」改成「1 commit」。這不在 Proposal 裡，是在 `review-round-economy` 的頁首發現的，當時已說明會在這個 Change 一起修。
4. **每頁 Review tab 開頭加上原本 Review 頁的 description。** 兩個檔案合併成一個之後，原本的第二份 frontmatter 放不進去；如果不這樣做，其中的摘要會消失。
5. **Change 頁的 tab 拿掉外框。** 你在 G2 做 S8 檢查時發現，fumadocs 給 tab 的外框、底色與內距，讓手機直立時的閱讀寬度太窄。依你的要求，所有寬度都拿掉外框與內距，tab 列也改為與內文左側對齊。

</Deviations>

<ScenarioResults
  scenarios={scenarios}
  results={[
    { id: "S1", result: "pass", evidence: "7 個 slug 在 dev server 上都回傳 200，兩個 tab 都在；舊的 `/proposal` 網址回傳 404" },
    { id: "S2", result: "pass", evidence: "瀏覽器確認各頁都有決策卡片與 scenario 卡片；`two-gate-workflow` 顯示 ADR-0045" },
    { id: "S3", result: "pass", evidence: "7 頁都顯示 `ScenarioResults`；逐字比對遷移前後，只少了表頭、列號、通過字樣與結果區塊的標題" },
    { id: "S4", result: "pass", evidence: "`blueprint-changes` 的測試：merge commit 與 squash 各一；7 頁 publish 後的 `mergedAt` 與合併時間相符" },
    { id: "S5", result: "pass", evidence: "既有的 facts 測試，另加 `mergedAt` 為 null 的斷言" },
    { id: "S6", result: "pass", evidence: "publish 本 Change 後，Blueprint deploy workflow 自動觸發並成功呼叫 hook；缺 secret 時失敗是 workflow 裡的明確檢查" },
    { id: "S7", result: "pass", evidence: "`pnpm verify:all` 四條 lane 全部通過；舊格式頁面在 dev server 上照常渲染" },
    { id: "S8", result: "pass", evidence: "開發者在桌面、手機、平板上實測：tab、toc、sidebar 都能操作，閱讀寬度足夠（拿掉 tab 外框之後）" },
  ]}
/>

<TestPlan
  items={[
    { id: "T1", checks: "publish 的 facts、workflow 同步、pull 過濾", method: "`pnpm test:workflow`", executor: "agent", environment: "本機", result: "pass" },
    { id: "T2", checks: "索引、頁首與元件", method: "blueprint 的 jest 與 `tsc --noEmit`", executor: "agent", environment: "本機", result: "pass" },
    { id: "T3", checks: "全部 gate", method: "`pnpm verify:all`", executor: "agent", environment: "本機", result: "pass" },
    { id: "T4", checks: "deploy hook 實際觸發", method: "publish 後查看 GitHub Actions 的 Blueprint deploy run", executor: "agent", environment: "GitHub Actions", result: "pass" },
    { id: "T5", checks: "遷移後的頁面在你慣用的瀏覽器與手機上可讀（S8）", method: "G2 時在 dev server 打開任一遷移頁面，看兩個 tab 與卡片；手機以同一網路連到 dev server", executor: "developer", environment: "本機 dev server", result: "pass", evidence: "桌面、手機、平板都沒問題" },
  ]}
/>

<ReviewDetails>

### Code review 的發現與處置

<RiskTable
  risks={[
    { name: "two-gate-workflow 的驗收表沒有轉成卡片，殘留風險也沒放進 ReviewDetails", severity: "warning", mitigation: "spec 第 1 輪發現：表格的 10 列其實能對應到 8 條 scenario。已依對應轉成卡片，每列的文字都保留，殘留風險一節也移進 ReviewDetails。" },
    { name: "Review 頁的 description 在合併時遺失", severity: "warning", mitigation: "每頁 Review tab 開頭改放原本的 description，`blueprint.md` 也寫明這一點。" },
    { name: "blueprint.md 的轉換規則為例外加了條件", severity: "info", mitigation: "刪掉「列數要與 scenario 相符」這個條件。" },
    { name: "設計檔查找的 helper 只剩一個呼叫者；落地範圍的欄位名稱與 facts.commits 撞名", severity: "info", mitigation: "helper 改為 inline；欄位改名為 `commitRange`。" },
    { name: "Change 分支前綴有兩份；applyChange 順帶處理 workflow 同步", severity: "info", mitigation: "前綴由 commitlint plugin 匯出，兩處共用；workflow 同步拆成獨立的 `syncDeployWorkflow`。" },
    { name: "store workflow 的原因在多處重複說明", severity: "info", mitigation: "原因只寫在 `syncDeployWorkflow`，其他地方改為指向它。" },
    { name: "手機直立時 tab 外框吃掉閱讀寬度", severity: "warning", mitigation: "G2 時由開發者發現。tab 在所有寬度都拿掉外框、底色與內距，tab 列與內文對齊；第 4、5 輪 review 只審這兩個 commit。" },
  ]}
/>

### 殘留風險

<RiskTable
  risks={[
    { name: "`dev` 的 push 與 store 的 publish 可能在短時間內各觸發一次建置", severity: "info", mitigation: "兩次建置拉到的內容相同，只是多花一次建置時間。之後若常發生，再改成 debounce。" },
    { name: "check-workflow 仍保留兩頁格式的 gate 檢查", severity: "info", mitigation: "現在已沒有兩頁格式的頁面，這段檢查不會再被觸發。Proposal 沒有把它列進範圍，留待之後清理。" },
    { name: "landingOf 依分支名稱或 trailer 辨認 Change", severity: "info", mitigation: "若 merge 時改寫了 subject、又沒有 trailer，會找不到落地 commit，退回用分支 diff 計算。7 個現有的 Change 都能正確辨認。" },
  ]}
/>

</ReviewDetails>

</Review>
</ChangeTabs>
