---
title: Standalone Decision Records
description: 決策記錄脫離交付流程，改住在一個扁平、去重複的目錄，schema 一併縮小。
capabilities: ["platform/blueprint","platform/delivery-workflow"]
---

export const scenarios = [
  { id: "S1", given: "a decision record naming two capabilities", when: "Blueprint builds", then: "it renders on both Feature pages from a single file, with no copy anywhere in the tree" },
  { id: "S2", given: "every file under blueprint/content/decisions/", when: "the record index is built", then: "each number occurs exactly once" },
  { id: "S3", given: "a record carrying only schemaVersion, id, title, capabilities and decision", when: "it is validated", then: "it passes" },
  { id: "S4", given: "a record under blueprint/content/decisions/ that still uses targets, originDecision, or schemaVersion 1", when: "the tree is validated", then: "validation fails and names the record" },
  { id: "S5", given: "a version 1 record inside a published Change page on the store branch", when: "that page renders", then: "it renders through the compatibility layer with its capabilities read from targets" },
  { id: "S6", given: "a capability whose page names no decisions", when: "that Feature page renders", then: "it renders with no decision timeline and no error" },
  { id: "S7", given: "a new decision record is written", when: "its number is chosen", then: "it is the highest number in blueprint/content/decisions/ plus one" },
  { id: "S8", given: "every record in blueprint/content/decisions/", when: "the directory is listed in lexical order", then: "the records come out in the order their numbers were assigned" },
  { id: "S9", given: "a Change that consulted no outside source", when: "its Proposal page is written", then: "it carries no References section, and no rule asks for an empty one" },
  { id: "S10", given: "a published Change page that throws while rendering", when: "Blueprint builds", then: "the build succeeds, that page shows a visible fallback naming it, and every other page renders its own content" },
  { id: "S11", given: "one decision record among several that fails to parse", when: "the timeline renders", then: "the records that parse still render and a visible note replaces the one that does not" },
];

<ChangeTabs>
<Proposal>

<TLDR>
決策記錄不再隸屬於任何一個 Change。所有記錄移到扁平的 `blueprint/content/decisions/`，編號於誕生時指派，沒有 per-capability 複本，也沒有 Archive 的提升步驟；Feature 頁改為依 `capabilities` 欄位過濾渲染，該欄位即原本的 `targets`。schema 的 required 欄位降到五個，刪除 `originDecision`，版本升到 2；已發佈在 store 分支上的 version 1 記錄則透過相容層繼續渲染。Fix path 可以修正一份記錄，但碰到 `decision` 本文或 `supersededBy` 就必須升級。編號是四位數零填充，扁平目錄因此照記錄產生的順序排序。同時落地兩條連帶的文件規則：研究筆記不自成檔案，以及 Fix path 的 `Refs:` trailer 改為條件性。
</TLDR>

## 為什麼是現在

三個事實，全部量自提案當時的檔案樹——遷移之後它們描述的是被取代的那個狀態：

1. `blueprint/content/features/**/decisions/` 底下有 **65 個檔案，內容只有 45 份不重複的記錄**。19 個編號出現超過一次，`D29` 出現三次。
2. **45 份全部**——每一份記錄所在的目錄集合，與它自己 `targets` 陣列裡的 ID 集合完全相同，零個例外。目錄樹沒有提供任何檔案本身沒說的資訊。
3. 每一組複本目前逐位元組相同（`$schema` 相對路徑除外）。也就是說它們**還沒**漂移，去重複是機械搬移，不是合併。

第二點才是論證。一份只是在複述欄位的複本，不是需要一致性檢查的冗餘，是需要刪掉的冗餘。

## 本次 Change 之後的形狀

<InteractiveFlowchart
  nodes={[
    { id: "discuss", label: "做出決定", sublabel: "在 Discuss 中，或任何 Change 之外", x: 90, y: 40, w: 190 },
    { id: "write", label: "寫下記錄", sublabel: "blueprint/content/decisions/", x: 330, y: 40, w: 190 },
    { id: "number", label: "指派編號", sublabel: "最大值 + 1，於誕生時", x: 570, y: 40, w: 180 },
    { id: "feature", label: "Feature 頁", sublabel: "依 capabilities 過濾", x: 330, y: 150, w: 190 },
    { id: "supersede", label: "被取代", sublabel: "supersededBy 指出後繼者", x: 570, y: 150, w: 190 },
  ]}
  edges={[
    { from: "discuss", to: "write" },
    { from: "write", to: "number" },
    { from: "write", to: "feature" },
    { from: "write", to: "supersede", label: "日後", dashed: true },
  ]}
  details={{
    write: {
      title: "一份決策，一個檔案",
      body: "沒有 proposal/decisions/ 目錄，沒有 per-capability 複本，Archive 也不再提升。記錄一寫下就是生效的：還沒定案的東西，本來就不會被寫成 ADR。",
    },
    feature: {
      title: "渲染改為推導而來",
      body: "一個 build-time index 讀取扁平目錄，做法與 blueprint/src/lib/changes-index.ts 讀取 Change 目錄的方式相同；每個 Feature 頁渲染 capabilities 精確命中自己的那些記錄。",
    },
    supersede: {
      title: "生命週期只有兩個事件",
      body: "誕生與被取代。沒有 status 欄位——過去那個角色是由目錄位置在扮演。",
    },
  }}
/>

## 本次 Change 的邊界

- 把 45 份不重複的記錄移到 `blueprint/content/decisions/D<n>-<slug>.json`，刪除 20 個複本檔案，並修正每份記錄的 `$schema` 相對路徑。
- 在 schema、手寫 validator、`DecisionTimeline` 與每一份記錄中，把 `targets` 更名為 `capabilities`。
- 編號改為四位數零填充：`id` 是 `0046`，檔名是 `0046-<slug>.json`，散文引用寫 `ADR-0046`。store 分支上的 version 1 記錄保留 `D<n>`，兩個版本各驗自己的 id 形狀。
- schema 升到版本 2：required 五個欄位，刪除 `originDecision`，`originChange` 保留且為選配。
- 把約十餘個 Feature `index.mdx` 的相對路徑 import，換成由 build-time index 支撐的 capability 過濾渲染。
- 擴充 legacy 相容層，把 version 1 記錄升版，讓 store 分支上早先 Change 頁裡的 48 份記錄繼續渲染。
- 讓單一 Change 頁的渲染失敗只弄壞那一頁：頁面 body 改以函式呼叫包在 try/catch 中（`output: "export"` 之下 React error boundary 不會被觸發，已實測），失敗時顯示可見的 fallback；決策渲染路徑本身改為 total。
- 依新模型改寫 `WORKFLOW.md` 的 Decision-record contract 與 `docs/agents/domain.md` 的 ADR 段落，並把 Fix path 的紅線改畫在 `decision` 與 `supersededBy` 兩個欄位上。
- 落地研究筆記規則：改寫 `docs/agents/artifact-lifecycle.md`，明定研究與工作筆記不自成檔案，以及 Proposal 頁 `## References` 段落的條件性與寫法。
- 把 Fix path 的 `Refs:` trailer 改為條件性，同步改寫 `WORKFLOW.md` Fix path 第 2、7 條與 `CONTRIBUTING.md` 的 trailer 段落。
- **不在範圍內** —— `CONTRIBUTING.md` 的程式碼註解引用規則仍在區分「Feature 編號」與「Change 自己的 `D2`」，而本 Change 消滅了這個區別。該檔案屬於 `reviewer-standards-split`，順序在後，由它承擔改寫。
- **不在範圍內** —— 已發佈在 `blueprint-changes` store 分支上、活在 Change 頁裡的那些記錄。它們是歷史，不遷移。
- **不在範圍內** —— ATE-167 剩下的兩項（CI 中的 schema 驗證、store 分支保留政策）。它的「同編號副本一致性」那一項被本 Change 直接消滅，而非解決；另外兩項等本 Change 落地後重新界定範圍。

## 行為契約

<Scenarios items={scenarios} />

## 風險

<RiskTable
  risks={[
    { name: "只收 version 2 的 parser 會在同一次 build 中弄垮每一個已發佈的 Change 頁", severity: "critical", mitigation: "ADR-0050：parseDecisionRecord 同時接受兩個版本，透過既有的 legacy 模組把 version 1 升版；store 分支永不改寫。" },
    { name: "capabilities 的值若不再對應到任何一頁，Feature 頁會無聲地少掉決策", severity: "critical", mitigation: "在驗證記錄的同一個測試裡，把每個 capabilities 值對照 Feature 樹驗證，對不上就失敗，而不是渲染出一個空頁。" },
    { name: "手工套用遷移，漏掉一份記錄或只改一半", severity: "warning", mitigation: "用 scripts/migrations/ 下的腳本遷移，並在 commit 前斷言 65 進 45 出、編號全數唯一。" },
    { name: "渲染從 MDX import 改為 build-time index，壞記錄失敗的時機也跟著變", severity: "warning", mitigation: "index 在 build 時解析每一份記錄，所以壞記錄一樣在 build 失敗，不會延到 request 時。" },
    { name: "G1 不再審「這個決策批不批」，決策沒有第二位讀者就進了 Features", severity: "info", mitigation: "ADR-0046 接受的代價：決策是在開發者在場的 Discuss 中做出的，gate 改審邊界與契約。" },
    { name: "並行的分支取到同一個下一號", severity: "info", mitigation: "編號在每個已合併狀態下唯一，唯一性斷言會在 gate 攔下碰撞；真的發生之前不做鎖。" },
    { name: "11 個 acceptance scenario，超過 8 個的 soft target 三條", severity: "warning", mitigation: "check:workflow 對此只警告不失敗。三條都是開發者在 G1 之後加進範圍的：一條來自 Ingest 的編號決定，兩條來自 S06 的 build 降級。拆分正是選擇 Ingest 與擴大範圍時各自否決過的做法，所以記錄超標而不拆——但這個 Change 已經比它被接受時大，那是 G2 該衡量的事實。" },
    { name: "本 Change 吸收了兩條文件規則，邊界從「決策記錄」擴到「Proposal 頁的寫法」", severity: "info", mitigation: "兩條規則都改在本 Change 已經要動的那幾個檔案裡，且都與 Fix path 的紅線同屬一次判斷；分成獨立 Change 會為三十行散文多開一次 gate。" },
  ]}
/>

## 決策

<DecisionCards ids={["0046", "0047", "0048", "0049", "0050", "0051", "0052", "0053", "0054"]} />

## 順序

四個之中排第一。其餘三個都要等它落地，才能用新模型寫自己的決策記錄。它與 `reviewer-standards-split` 在 `CONTRIBUTING.md` 的註解引用規則上交棒——本 Change 消滅那個區別，後者執行改寫。

## References

蒐集於 2026-09-19 的討論。**一份 reference 記錄的是它被讀到那天為真的事**，所以每一列都要說出自己的強度，以及它能支持到什麼程度。〈為什麼是現在〉裡的數字是對本 repo 的量測，不是 reference，因此留在該節。

| 來源 | 支持什麼 | 強度 |
| --- | --- | --- |
| 已安裝的 `domain-modeling` playbook 與其 ADR 格式 | ADR 住在 `docs/adr/<n>-<slug>.md`，編號靠掃描目錄取最大值加一，在決策發生的當下寫下，而且可以只有一段話——status、options、consequences 全是選配。 | **高**。本 repo 其餘部分本來就遵循的 playbook，直接讀已安裝的版本；它是 ADR-0046 與 ADR-0048 形狀的來源。 |
| 同一套的 `to-spec` 與 `to-tickets` | 它們只叫 agent **respect** 所觸碰範圍內既有的 ADR。既不產生也不擁有，所以決策記錄與交付單位之間沒有任何關係。 | **高**。這是分開兩個模型的那一句話，而且寫得很明確。 |
| 同一套的 `grill-with-docs` | 決策在產生它的那場 grilling 當中就寫下，不等任何階段結束。 | **高**。它全文只有一句指令：透過 `domain-modeling` 跑 grilling。 |
| 找不到外部來源 | 這套 playbook 沒有談「在 gate 上審一個決策」，因為它根本沒有 gate。 | **低**，而且是誠實的缺口：拿掉 proposed 階段是本 repo 自己的判斷，由 ADR-0046 的 consequences 承擔，不是讀來的。 |

## Ingest 註記

這一頁在 G2 之後被退回改寫過一次。開發者授權 Ingest，把編號改成四位數零填充——理由是量到的缺陷：53 份記錄進同一個目錄之後，`D10` 排在 `D2` 前面，而舊模型下每個目錄只有兩到六個檔案，排序從未重要過。位置、欄位與檔案類型都沒有動；`ADR-0054` 記錄了這個決定。已完成的 slice 與它們的證據全部保留，`0054` 是第九份記錄。

## Bootstrap 註記

最初那八份記錄誕生在它們要取代的那個模型底下：活在某個 Change 的 `proposal/decisions/` 裡，version 1，帶著 `targets` 與填滿的九個欄位。一條讓決策記錄脫離交付流程的規則，沒辦法由一份已經脫離了的記錄來記載。它們在本 Change 自己的 Apply 階段，與另外 45 份走同一趟遷移，移到 `blueprint/content/decisions/`、轉成 version 2 並取得 `0046`–`0053`——那是一份記錄一生中唯一一次改編號。這是 bootstrap 該有的樣子，不是模型的缺陷。

</Proposal>
<Review>

<TLDR>
65 個檔案成為 54 份記錄，住在扁平的 `blueprint/content/decisions/`，編號四位數零填充；Feature 頁改依 `capabilities` 過濾渲染，不再逐條 import。schema 升到 version 2（required 五個欄位、刪除 `originDecision`），store 分支上的 version 1 記錄透過相容層繼續渲染。本 Change 在 G2 之後被退回一次（Ingest：編號零填充），並在第二次 G1 之後擴大範圍一次（S06：單一頁面的渲染失敗不再弄壞整個 build）。11 條 acceptance scenario，9 條由測試或實測證明。四輪兩軸 review，九項發現全部處置。
</TLDR>

<ScenarioResults
  scenarios={scenarios}
  results={[
    { id: "S1", result: "pass", evidence: "一份記錄命名兩個 capability，兩頁都渲染，樹中無複本。`decisions-tree.test.ts` 的「leaves no per-capability copy behind」；瀏覽器確認 `platform/blueprint` 與 `platform/delivery-workflow` 各自渲染同一份" },
    { id: "S2", result: "pass", evidence: "每個編號恰好出現一次。`decisions-tree.test.ts`「uses each number exactly once」，54 個檔案" },
    { id: "S3", result: "pass", evidence: "只帶五個必填欄位的記錄通過驗證。同檔「accepts a record that is only a title, a capability and a decision」" },
    { id: "S4", result: "pass", evidence: "扁平目錄下仍帶 `targets`／`originDecision`／version 1 的記錄驗證失敗並指名。同檔 `it.each` 拒絕表" },
    { id: "S5", result: "pass", evidence: "store 分支已發佈頁裡的 version 1 記錄仍渲染，`capabilities` 讀自 `targets`。review 代理 fetch `origin/blueprint-changes`，把帶 `status` 與不帶 `status` 的真實記錄各沿渲染路徑追過一次" },
    { id: "S6", result: "pass", evidence: "沒有記錄的 capability 渲染時沒有 timeline 也不報錯。`DecisionTimeline.test.tsx`「renders nothing for a capability that names no decisions」" },
    { id: "S7", result: "unverified", evidence: "新記錄的編號是目錄最大值加一。未證明：寫入當下的紀律，沒有程式路徑可驗；見殘留風險" },
    { id: "S8", result: "pass", evidence: "目錄以字典序列出時，順序等於編號指派的順序。`ls blueprint/content/decisions` 的字典序輸出為 `0001`…`0054`，這正是 Ingest 要修的缺陷" },
    { id: "S9", result: "unverified", evidence: "沒有外部來源的 Change 不帶 References 段落。未證明：撰寫規則，由 `docs/agents/artifact-lifecycle.md` 承載，無可執行路徑" },
    { id: "S10", result: "pass", evidence: "已發佈的 Change 頁渲染中拋錯時，build 成功、該頁顯示 fallback、其他頁正常。MDX：review 代理自建 gitignored 的廢棄 Change 目錄使其拋錯，build 成功 138 頁、壞頁顯示中文 fallback、其他頁零 fallback。Mockup：在 `api-objectid-guards/design.tsx` 植入 sentinel throw，未包 MockupFrame 前 build 中止於該頁，包上之後 138 頁全過、瀏覽器顯示 fallback；兩者事後皆還原，工作樹乾淨" },
    { id: "S11", result: "pass", evidence: "多筆記錄中有一筆解析失敗時，其餘照常渲染，失敗處留下可見註記。`DecisionTimeline.test.tsx` 的一好一壞混合案例" },
  ]}
/>

## 驗證

| 檢查 | 結果 |
| --- | --- |
| `pnpm verify:all` | `static` 與 `blueprint` 通過；`app-build`／`app-test` 因 diff 未觸及 app `src` 而跳過 |
| `pnpm --filter blueprint test` | 14 suites，64 tests |
| `pnpm check:workflow` / `pnpm test:workflow` | 通過／63 tests |
| 乾淨重建 | `rm -rf .next out` 後 build，零個 prerender error、零個 fallback |
| 遷移斷言 | 65 檔入、54 筆出（45 既有 + 本 Change 的 9 筆），編號唯一，複本內容一致 |
| 重新編號查證 | `git diff --find-renames` 顯示 45 筆為純改名（相似度 84–98%），每個 `supersededBy` 仍指向同一決策 |

規模：19 個 commit，106 個檔案，**刪除的行數多於新增**（1,431 / 1,578）。

## 邊界上最關鍵的一段 diff

三次紅燈的共同結構是：**Change 頁在自己的 schema 與元件合併之前就必須發佈**，而 gate 的定義使這件事無法避免。真正的修法不是讓頁面自我克制，而是讓 build 承受得住。

<AnnotatedDiff lang="tsx" unified code={" function renderChangeBody(\n   Mdx: NonNullable<NonNullable<SourcePage>[\"data\"][\"body\"]>,\n   title: string,\n ) {\n-  return <Mdx components={mdxComponents} />;\n+  try {\n+    return Mdx({ components: mdxComponents });\n+  } catch (error) {\n+    console.error(`Change page \"${title}\" failed to render:`, error);\n+    return (\n+      <p className=\"text-sm text-destructive\">\n+        此頁面（{title}）在此 checkout 中無法顯示：\n+        {error instanceof Error ? error.message : String(error)}\n+      </p>\n+    );\n+  }\n }"} />

design mockup 走的是另一套機制：它持有 hooks，無法當函式呼叫；而 error boundary 在 prerender 期間不會被觸發（同一個 sentinel 實測），所以 `MockupFrame` 把它延到瀏覽器才渲染。

<ReviewDetails>

### Code review 的發現與處置

四輪兩軸。第四輪只有前一輪逐字指定的內容，依契約的不動點定義不再重開迴圈。

<RiskTable
  risks={[
    { name: "已發佈的 Proposal 頁三度讓全 repo 的 PR 紅燈", severity: "critical", mitigation: "根因是 Change 頁必須在自己的 schema 與元件合併之前發佈。先降級發佈內容解除阻塞，再由 S06 讓單一頁面的失敗只弄壞那一頁。" },
    { name: "第一個 commit 混入 2,857 行無關的 vendor 檔案", severity: "critical", mitigation: "起因是用 git add -A 修改 commit。分支未推，以 git rebase --onto 重做該 commit 並重放其餘八個；dev...HEAD 現在零命中。此後一律指名路徑。" },
    { name: "React error boundary 在 output: export 之下不會被觸發", severity: "warning", mitigation: "以 sentinel log 實測證明 getDerivedStateFromError 從未被呼叫，error.tsx 同樣無效。改以函式呼叫加 try/catch，讓頁面 body 的 render 落在 catch 的同一個 call stack 上。" },
    { name: "parseDecisionRecord 把兩個 schema 版本糊在同一條 35 行布林鏈裡", severity: "warning", mitigation: "拆成 hasValidBody 與各版本自己的 key 集合與 id 形狀；下一輪逐欄位比對 schema 確認無任何放寬。" },
    { name: "S06 的隔離只包了兩道 gate 的 render 路徑，四個 legacy 分支未包", severity: "warning", mitigation: "代理誠實回報而未擴大範圍。三個純 MDX 分支直接補上；design mockup 當時以「它是本 checkout 的 TSX」為由豁免，該理由經查為誤——mockup 與其他 Change 頁一樣由 store 分支拉進來。改以 MockupFrame 覆蓋，四個分支現皆受保護。" },
    { name: "Feature 頁在 render 裡建 MDX component map", severity: "warning", mitigation: "改為依 capability 快取一次，identity 在共用同一 capability 的頁面之間保持穩定。" },
    { name: "renderChangeBody 的註解比它描述的程式碼長", severity: "info", mitigation: "砍成結論，調查過程留在跑那次調查的 commit body。" },
    { name: "originDecision 的拒絕斷言同時存在於兩個測試檔", severity: "info", mitigation: "只留在 parser 自己的 suite。" },
    { name: "遷移腳本已無法再執行——來源目錄不存在", severity: "info", mitigation: "刪除。它做了什麼、斷言了什麼，commit 與訊息留著。" },
  ]}
/>

### 殘留風險

<RiskTable
  risks={[
    { name: "11 個 acceptance scenario，超過 soft target 三條", severity: "warning", mitigation: "三條都是 G1 之後加進範圍的：一條來自 Ingest，兩條來自 S06。拆分正是兩次擴大範圍時各自否決過的做法。這個 Change 已經比它被接受時大，那是 G2 該衡量的事實。" },
    { name: "Scenario 7 未被任何程式證明：編號取 max+1 是寫入當下的紀律", severity: "warning", mitigation: "沒有補一個沒人呼叫的 helper 來假裝它可驗。現有護欄是唯一性斷言——抓得到撞號，抓不到跳號。" },
    { name: "已發佈的 Proposal 頁目前不渲染自己的九份記錄", severity: "warning", mitigation: "dev 的 parser 還只認得 version 1。頁面上有一段引文說明原因。合併後補上渲染並重新發佈——S06 之後這不再是 build 風險，只是暫時的顯示缺口。" },
    { name: "mockup 改為只在瀏覽器渲染，不再進 static HTML", severity: "info", mitigation: "error boundary 在 prerender 期間無效（已實測：throw 仍中斷 build），所以 MockupFrame 把 mockup 延到掛載後才渲染。代價是設計稿內容需要 JS 才看得到，且壞掉的 mockup 只在瀏覽器現形——對一個內部文件站可接受。" },
    { name: "Feature 頁的「Implemented decisions」標題寫在 MDX 裡，timeline 空掉時標題會孤懸", severity: "info", mitigation: "目前每個帶該標題的頁面都有記錄。根治要把標題也交給元件，那會改動 12 頁的 TOC。" },
  ]}
/>

</ReviewDetails>

</Review>
</ChangeTabs>
