---
"volleybro": patch
---

### Added

#### Infrastructure

- The Blueprint has a home page with entries to Features, Changes and the Design System

### Changed

#### Infrastructure

- The Changes index shows each Change as a card with its state and date, newest first across both page formats
- A Change page's figures date it by `startedAt` and `archivedAt`, replacing `mergedAt`
