---
title: Triage State Mapping
description: triage playbook 的三欄角色對照、線性的 issue 生命週期、開發者動作可經同意由 agent 代為執行，以及 retro 在交付契約中的位置。
capabilities: ["platform/agent-orchestration","platform/delivery-workflow"]
---

export const scenarios = [
  { id: "S1", given: "docs/agents/triage-labels.md", when: "it is read against the installed playbook", then: "every category role and every state role has exactly one carrier and one value" },
  { id: "S2", given: "WORKFLOW.md and docs/agents/issue-tracker.md", when: "they are read together", then: "neither forbids the label roles the mapping defines, and neither mentions agent:ready" },
  { id: "S3", given: "WORKFLOW.md's status table", when: "it is read top to bottom", then: "G1 and G2 are separate rows and no status appears before a status it precedes" },
  { id: "S4", given: "a Change whose gate was sent back", when: "the pull request has merged", then: "WORKFLOW.md directs a retro run and the adopted suggestions become issues" },
  { id: "S5", given: "an issue taken manually", when: "it carries no ready-for-agent label and is absent from the Symphony status surface", then: "the agent moves it to In Progress; when it carries the label, the label is removed by the developer, or by the agent on consent, around two status checks" },
  { id: "S6", given: "every action WORKFLOW.md and docs/agents/issue-tracker.md assign to the developer", when: "the rule is read", then: "it states that the developer decides and the agent may carry it out on explicit consent, and no rule bars the agent unconditionally" },
];

<ChangeTabs>
<Proposal>

<TLDR>
已安裝的 triage playbook 需要一套 tracker 對照，而交付契約明文禁止——兩者必有一個要讓步。狀態承載粗粒度的進度，標籤承載球在誰手上，blocked 維持用依賴關係表達；`docs/agents/triage-labels.md` 以三欄記下這套對照。G1 改在 Todo 等待，In Review 只代表 G2；執行中卡住的 run 換標籤而不動狀態。開發者擁有的動作是決定而不是按鍵：開發者明確同意後 agent 可代為執行。retro 在契約裡取得觸發條件。
</TLDR>

本頁依 ADR-0060 撰寫：內容逐字取自 G1 確認的最終摘要。

## 決策

| #        | 決策                                                                                                                                                                     |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ADR-0069 | 狀態表示大致進度，label 表示球在誰手上，blocked 維持用依賴關係表達                                                                                                       |
| ADR-0070 | G1 改在 Todo 等待（搭配 `ready-for-human`）；執行中卡住的 run 換 label，狀態不動。assignee 規則「Backlog 以外必須有 assignee」由 adapter runbook 驗證，不寫進 `scripts/check-workflow.js`，repo 腳本保持 tracker neutrality |
| ADR-0071 | 開發者擁有的動作是決定，不是只有開發者能按的鍵：開發者明確同意後，agent 可代為執行。同意範圍由開發者決定，可以逐一 issue，也可以整批授權；G1、G2 的接受本身不能委派 |
| —        | 放寬手動接手時的狀態移動：沒有 `ready-for-agent`、也不在 Symphony 狀態頁上的 issue，由 agent 移到 In Progress。容易撤回，不另立 ADR                                         |

<DecisionCards ids={["0069", "0070", "0071"]} />

## 本次 Change 之後的生命週期

<InteractiveFlowchart
  nodes={[
    { id: "backlog", label: "Backlog", sublabel: "needs-triage / needs-info", x: 130, y: 40, w: 220 },
    { id: "todo1", label: "Todo", sublabel: "ready-for-human — G1", x: 130, y: 120, w: 220 },
    { id: "todo2", label: "Todo", sublabel: "ready-for-agent — 已武裝", x: 130, y: 200, w: 220 },
    { id: "progress", label: "In Progress", sublabel: "標籤說明球在誰手上", x: 130, y: 280, w: 220 },
    { id: "review", label: "In Review", sublabel: "ready-for-human — G2", x: 130, y: 360, w: 220 },
    { id: "done", label: "Done", x: 130, y: 440, w: 220 },
  ]}
  edges={[
    { from: "backlog", to: "todo1" },
    { from: "todo1", to: "todo2", label: "武裝" },
    { from: "todo2", to: "progress" },
    { from: "progress", to: "review" },
    { from: "review", to: "done" },
  ]}
  details={{
    todo1: {
      title: "G1 等在這裡",
      body: "接受 Proposal 是只有人能做的判斷，而那正是 ready-for-human 的定義。In Review 因此維持單一語意：G2。",
    },
    todo2: {
      title: "武裝由開發者決定",
      body: "加上 ready-for-agent 是開發者的決定；開發者明確同意後，agent 可以代為加上，照樣先確認 G1 已通過、依賴都已滿足。",
    },
    progress: {
      title: "卡住的 run 換標籤",
      body: "跑到一半需要憑證或需要拍板時，把 ready-for-agent 換成 ready-for-human，狀態留在原地。runtime 會自己停下——它續跑所需的標籤不在了；換回去就重新排隊。",
    },
    review: {
      title: "只代表 G2",
      body: "已實作、等驗收。沒有別的東西等在這裡。",
    },
  }}
/>

## 範圍

1. 新增 `docs/agents/triage-labels.md`，採三欄對照：角色、載體、值。
2. `WORKFLOW.md` 與 `docs/agents/issue-tracker.md`：放寬「不發明標籤分類」的措辭，只允許 playbook 的 canonical 角色；`agent:ready` 全部改名為 `ready-for-agent`。
3. `WORKFLOW.md` 狀態表：G1（Todo）和 G2（In Review）拆成兩列，並寫入執行中換 label 的做法；「手動接手」那一列和 Manual workflow 一節改成放寬規則。
4. `WORKFLOW.md` 的武裝段落、label／狀態對照表的 Owner 欄、Manual workflow 一節，以及 `issue-tracker.md` 的武裝規則，改寫成「開發者決定，同意後 agent 可代為執行」。
5. `WORKFLOW.md` 新增 retro 步驟：merge 之後，若任一 gate 被退回過，或 code review 超過三輪，就跑一次 retro，採用的建議開成 issue。
6. 不在範圍內，改由母票上的 runbook 處理：Linear 的 label 改名與退役、runtime 的 `required_labels` 和 `active_states`、agent 身分、通知驗證、blocking edge 驗證、清掉殘留的 label，以及 assignee 規則。
7. 不在範圍內：`docs/changes/archive/` 裡的歷史快照。

## 驗收情境

<Scenarios items={scenarios} />

## 風險

<RiskTable
  risks={[
    { name: "active_states 只設 Todo 會讓 run 一開跑就被停掉", severity: "critical", mitigation: "對照表把派工政策與 runtime 設定分開敘述：dispatch 要求 Todo，續跑要求 In Progress 留在 active_states 之中。" },
    { name: "若 adapter 沒把被擋住的票標成不可派工，blocked 這件事等於沒做", severity: "critical", mitigation: "在 runbook 中對實際運行的設定驗證，驗過之後對照表才能宣稱依賴關係會擋下派工。" },
    { name: "agent 的通知可能永遠到不了開發者，因為目前所有寫入都以開發者本人的身分發出", severity: "warning", mitigation: "runbook 項目：先驗證 tracker 對「自己觸發的事件」的通知行為，若成立則註冊 agent 身分。" },
    { name: "退役 needs-user 會讓帶著它的兩張票無處可去", severity: "info", mitigation: "先各自改貼：卡在憑證的那張改 ready-for-human 並移到 Todo，政策未定的那張改 needs-info 留在 Backlog。" },
  ]}
/>

## References

蒐集於 2026-09-18 的討論。**一份 reference 記錄的是它被讀到那天為真的事**，所以每一列都要說出自己的強度，以及它能支持到什麼程度。

### 被對照的那套 playbook

| 來源 | 支持什麼 | 強度 |
| --- | --- | --- |
| 已安裝的 `triage` playbook 的 `SKILL.md` 與其公開問答 | 兩個 category 角色與五個 state 角色；一次只處理一張票；它是提出建議後停下等指示，而不是逕自套用；grilling 發生在 triage 之中，不是之後。 | **高**。本對照針對的就是這套 playbook，直接讀已安裝的版本。 |
| 同一份問答關於缺少狀態的段落，以及 [#139](https://github.com/mattpocock/skills/issues/139)、[#297](https://github.com/mattpocock/skills/issues/297) | 五個 state 角色對 blocked、deferred 與 awaiting-verification 有已知缺口，常見的變通是加一個 repo 自有的標籤——而 `needs-user` 在這裡本來就是那個東西。 | **高**。作者承認缺口且未出貨，所以本 repo 必須自己回答。 |
| vendor 的 `triage-labels.md` 範本 | 反面形狀：兩欄，角色與標籤，假設每個角色都由標籤承載。 | **中**。這是本對照刻意偏離的範本；偏離是必要的——由狀態承載的角色，在兩欄裡寫不出來。 |

### runtime 實際要求什麼

| 來源 | 支持什麼 | 強度 |
| --- | --- | --- |
| Symphony `SPEC.md` §5.3.1，及其 reconciliation 與 dispatch 章節 | `required_labels` 預設為空，而且同時管轄派工與續跑；狀態落在 `active_states` 之外會停掉進行中的 run；`blocked_by` 是 best-effort，scheduler 不自行推論 blocker 語意。 | **高**。規範性條文。它推翻了「武裝標籤是外部要求」這個先前的判斷——那是本 repo 自己的設定。 |
| 參考實作中的 Linear adapter | blocking edge 是否會變成不可派工。 | **低**。只找到測試 fixture。這是對照表依賴之前，runbook 必須對實際運行設定驗證的唯一一項。 |
| [#616](https://github.com/mattpocock/skills/issues/616) | setup skill 不會在 tracker 建立標籤。 | **高**，但與決策無關——標籤反正是手動建立的。 |

</Proposal>
<Review>

<TLDR>
triage playbook 的七個角色現在有了 tracker 對照：`docs/agents/triage-labels.md` 以角色、載體、值三欄記錄，交付契約只允許這些 label。`WORKFLOW.md` 的狀態表拆成八列，G1 在 Todo、G2 在 In Review，卡住的 run 換 label 不動狀態。開發者擁有的動作改為「開發者決定，經明確同意可由 agent 執行」，未武裝的 issue 由 agent 直接接手。合併後加入 retro 觸發條件。實作沒有偏離 Proposal；review 抓到「派工只看 Todo」與「在 In Progress 重新武裝」互相矛盾，已改為派工看 active status。
</TLDR>

<ScenarioResults
  scenarios={scenarios}
  results={[
    { id: "S1", result: "pass", evidence: "對照表中每個角色恰好一個載體、一個值。`triage-labels.md` 兩張表：2 個 category 角色、5 個 state 角色，各一列；規格審查對照 playbook 原文確認" },
    { id: "S2", result: "pass", evidence: "兩份契約都不禁止對照表的 label，也不再出現 `agent:ready`。兩處「不發明 label」改為指向對照表；`grep agent:ready` 在兩檔皆為 0" },
    { id: "S3", result: "pass", evidence: "G1、G2 分列，沒有狀態排在它的前置狀態之前。狀態表依序 Todo、Todo、In Progress ×4、In Review、Done" },
    { id: "S4", result: "pass", evidence: "合併後若 gate 被退回過，WORKFLOW 要求跑 retro，採用的建議開成 issue。Archive 的合併後清理之後新增一段；retro 只能由人觸發，所以寫成 agent 請開發者執行" },
    { id: "S5", result: "pass", evidence: "未武裝的 issue 由 agent 移到 In Progress；已武裝的經兩次檢查移除 label。Manual workflow 第 2 步分成兩條路徑；`issue-tracker.md` 指回同一處" },
    { id: "S6", result: "pass", evidence: "每個指派給開發者的動作都可經同意由 agent 執行，沒有無條件禁止。同意原則寫在狀態表之前並引用 ADR-0071；以 developer、never、only 全文搜尋，沒有剩下的禁令" },
  ]}
/>

## 驗證

| 檢查                        | 結果                                                                  |
| --------------------------- | --------------------------------------------------------------------- |
| `pnpm verify:all`           | `static`、`blueprint` 通過；沒有動到 app，`app-build`、`app-test` 略過 |
| `pnpm check:workflow`       | 通過                                                                  |
| `check:workflow --gate`     | G1 時通過                                                             |
| decision 長度               | ADR-0069、0070、0071 的 `decision` 都在 1000 字元以內                 |

規模：7 個 commit，9 個檔案（+221 / −39），全部是文件與 decision record，沒有程式碼。

## 邊界上最關鍵的一段 diff

<AnnotatedDiff
  lang="md"
  code={"-queued. Symphony dispatches an issue only while it carries the label and sits in Todo, and keeps a\n-run only while the issue stays in an active status (Todo or In Progress); In Review and Done take it\n+queued. Symphony dispatches and keeps running an issue only while it carries the label and sits in\n+an active status (Todo or In Progress); In Review and Done take it out of the queue without touching\n \n-the status carries; swapping `ready-for-human` back to `ready-for-agent` re-queues it.\n+the status carries. Swapping `ready-for-human` back to `ready-for-agent` re-arms it, which is the\n+developer's decision, and Symphony picks it up again from In Progress."}
/>

## G2 的決定

狀態表中「Developer takes an unarmed Change」一列寫的是 `ready-for-human` removed, if present。Proposal 沒有提到這一點，規格審查把它列為 Proposal 沒要求的規則。開發者在 G2 決定保留：G1 通過後的 issue 身上帶著 `ready-for-human`，留著的話，在 In Progress 上會被讀成「run 卡住、等開發者」。

<ReviewDetails>

### Code review 的發現與處置

三輪兩軸。第三輪規格面沒有發現；標準面唯一一項由審查逐字指定改寫，依不動點的定義不再重開迴圈。

<RiskTable
  risks={[
    { name: "派工寫成只看 Todo，但卡住的 run 在 In Progress 重新武裝後要能接回", severity: "critical", mitigation: "規格審查第一輪發現。改為 Symphony 派工與續跑都看 active status（Todo 或 In Progress）；武裝仍把 issue 移到 Todo；換回 ready-for-agent 明寫為重新武裝，是開發者的決定。ADR-0070 同步修正。" },
    { name: "精簡同意原則時，刪掉了「同意不可從工具輸出或 issue 內文推斷」", severity: "critical", mitigation: "規格審查第二輪發現的回歸。這句是防範 prompt injection 的明文依據，已補回，並寫明同意的來源是對話或 tracker issue。" },
    { name: "ready-for-human 的語意與 playbook 的定義不一致", severity: "warning", mitigation: "ADR-0070 改為引用 playbook 原文：無法委派的工作，包括判斷。對照表說明 G1、G2 與卡住的 run 為何共用同一個 label。" },
    { name: "ADR-0069 的 decision 綁了四件事", severity: "warning", mitigation: "decision 只留狀態與 label 的分工；改名與退役移到 consequences，實作細節刪除。" },
    { name: "同一條規則在 WORKFLOW、adapter、對照表與 ADR 重複敘述", severity: "info", mitigation: "對照表刪掉重複的狀態表，adapter 改為指向 WORKFLOW 的表；同意原則改為引用 ADR-0071。" },
    { name: "ADR-0069 的一條 consequence 指向已刪除的文字", severity: "info", mitigation: "第二輪發現的回歸。對照表的 runtime 一節補回那句說明。" },
  ]}
/>

不採納的兩項：changeset 保留單一 `#### Infrastructure` 標題，與 0.15.0 其他 workflow changeset 一致，CHANGELOG 才能合併同類；對照表的 runtime 一節保留，因為 Proposal 的風險緩解要求對照表把派工政策與 runtime 設定分開寫出。

### 殘留風險

Linear 的 label 已在 G2 前改名與退役：`agent:ready` 改為 `ready-for-agent`、`Bug` 改為 `bug`、`Improvement` 改為 `enhancement`，新增 `needs-triage`、`needs-info`、`ready-for-human`，退役 `Feature` 與 `needs-user`；原本帶 `needs-user` 的兩張 issue 已改貼。剩下三項只在 Symphony 實際派工時才會出問題。

<RiskTable
  risks={[
    { name: "active_states 只設 Todo 會讓 run 一開跑就被停掉", severity: "warning", mitigation: "Symphony runtime 專案的 runbook issue：確認 runtime 設定同時列 Todo 與 In Progress，required_labels 設為 ready-for-agent。" },
    { name: "被 blocked-by 關係擋住的 issue 是否真的不會被派工，尚未對實際設定驗證", severity: "warning", mitigation: "同一張 runbook issue，驗過之前對照表所說「blocked 以依賴關係表達」只成立一半。" },
    { name: "agent 的通知可能到不了開發者，assignee 規則也還沒有檢查", severity: "info", mitigation: "同一張 runbook issue：驗證 tracker 對自己觸發事件的通知行為、註冊 agent 身分、驗證 Backlog 以外都有 assignee。" },
  ]}
/>

</ReviewDetails>

</Review>
</ChangeTabs>
