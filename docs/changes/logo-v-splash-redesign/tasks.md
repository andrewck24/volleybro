## 1. Mark geometry in components

- [x] 1.1 Rebuild public/brand/logo-symbol.svg and public/brand/logo-type.svg from the Saira Stencil (variable) lowercase `v` at wght 700 scaled to cap height (two-arm split, ivory/coral fills, letters translated +6.59). Verified by rendering both SVGs on a teal ground and inspecting shape, alignment, and optical gap. Re-run the extraction and update both SVGs if the weight explorer settles on a different weight.
- [x] 1.2 Implement the "Mark glyph geometry" requirement in src/components/brand/logo-symbol.tsx per the design decision "Lowercase v scaled to cap height replaces the uppercase V": replace `V_ARM_LEFT` / `V_ARM_RIGHT` with the lowercase-v paths and add an exported viewBox constant (e.g. `V_VIEWBOX = "-7 -27 564 564"` at wght 700) used by the component's own `<svg>`. Behavior: `LogoSymbol` renders the new near-square mark, keeping the "Two-arm color contract" (`variant` semantics unchanged: adaptive neutral = currentColor, brand neutral = `#F6F4F5`, right arm always `#FC7A56`). Verify: `pnpm verify` (typecheck + existing component tests) passes and the mark renders correctly in the running app header.
- [x] 1.3 Apply the same "Mark glyph geometry" requirement to src/components/brand/logo-type.tsx: swap the V arm paths for the cap-height lowercase-v arms and wrap the untouched letter paths in a group translated +6.59 with viewBox widened to `0 0 546.5 115` (values re-derived if the pinned weight changes), and export the wordmark geometry (v arm paths, letter path list, letter shift, viewBox) as named constants for the splash route. Behavior: wordmark renders with the new mark at cap height, letters unchanged, colors per the "Two-arm color contract". Verify: `pnpm verify` passes and the wordmark renders correctly on light and dark themes.

## 2. Apple splash route

- [ ] 2.1 Update the "V mark composition" of the apple splash per the design decision "Mark geometry exported once, viewBox included", satisfying the "Single source of truth for geometry" requirement: replace the hardcoded `viewBox="-10 225 360 360"` in src/app/apple-splash/[size]/route.tsx with the viewBox constant imported from the brand module, and additionally render the wordmark from the exported logo-type geometry, centered at 40% of the shorter dimension wide with its bottom edge inset 6% of the screen height (neutral parts ivory, right arm coral). Behavior: a request for a registered size returns a PNG of exactly that size with the new mark centered at 25% of the shorter dimension on `#10687E` and the wordmark near the bottom; the route source contains no mark geometry literals. Verify: existing tests in src/app/apple-splash/**tests** pass (update any assertion pinned to the old viewBox/paths), plus a manual request of one registered size in `pnpm dev`.

## 3. Android splash and icons

- [ ] 3.1 [P] Implement "Manifest splash background matches the brand" and the manifest half of "Maskable splash icon reads as a bare V", per the design decision "Android splash driven by manifest colors plus maskable icons": set `"background_color": "#10687e"` in public/manifest.json and add two icon entries with `"purpose": "maskable"` (192x192, 512x512) pointing at public/maskable-icon-192x192.png and public/maskable-icon-512x512.png. Behavior: Chrome DevTools Application → Manifest shows no maskable warning and previews the splash as teal with a bare V. Verify: DevTools manifest panel check on `pnpm dev`.
- [ ] 3.2 [P] Implement "Raster icons derive from the mark" per the design decision "Raster icons regenerated by a committed script": add `sharp` as a devDependency and create scripts/generate-icons.mjs that renders every raster icon (favicon.ico, icon-_, android-chrome-_, apple-touch-icon*, maskable-icon-*) from the brand SVG geometry — maskable variants get a full-bleed `#10687E` background with the mark inside the 80% safe zone, completing "Maskable splash icon reads as a bare V". Behavior: running the script on a clean checkout deterministically rewrites all raster icons and every path referenced by public/manifest.json and src/app/layout.tsx exists afterwards. Verify: run the script, confirm the file list, and visually inspect the 512 maskable and 512 any icons.
- [ ] 3.3 Run scripts/generate-icons.mjs and commit the regenerated icon set. Behavior: all public/ raster icons show the new lowercase-v mark. Verify: visual inspection of regenerated PNGs and favicon.

## 4. Blueprint pages

- [ ] 4.1 [P] Refresh the frozen copy blueprint/src/components/brand/ (logo-symbol.tsx, logo-type.tsx) to the new geometry. Behavior: the blueprint brand page (design-system/brand) renders the new mark in all three grounds. Verify: `pnpm verify:all` builds the blueprint and the page renders the new mark.
- [ ] 4.2 Implement the design decision "Blueprint design page with variant and splash mockups": create blueprint/content/changes/in-progress/logo-v-splash-redesign/ (meta.json, index.mdx with ChangeOverview, design.mdx stub, design.tsx) and register it in blueprint/content/changes/in-progress/meta.json `pages` plus the `designModules` map in the changes catch-all route page. Behavior: the design page renders logo-symbol and logo-type mockups on the three grounds (light/dark/teal) and phone-frame splash mockups for iOS and Android. Verify: `pnpm verify:all` builds the blueprint and the page renders with working TOC.

## 5. Verification

- [ ] 5.1 Run `pnpm verify:all`; fix any failures. Behavior: format, typecheck, lint, tests, app build, and blueprint build all pass with the new mark everywhere. Verify: command exits 0.
