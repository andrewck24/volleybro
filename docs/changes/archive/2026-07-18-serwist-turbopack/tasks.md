## 1. Route handler and config

- [x] 1.1 Create src/app/serwist/[path]/route.ts calling createSerwistRoute({ swSrc: "src/app/sw.ts", useNativeEsbuild: true }) and re-exporting its GET handler, generateStaticParams, and static-rendering directives (dynamic "force-static", dynamicParams false, revalidate false). Implements design Decision 1: route-handler generation with useNativeEsbuild. Done when the file typechecks and next build prerenders the /serwist/sw.js path, satisfying the requirement "Service worker generated by the Next.js build".
- [x] 1.2 Wrap the Next config with withSerwist from @serwist/turbopack in next.config.js, composed as withBundleAnalyzer(withSerwist(nextConfig)). Done when next build succeeds with both wrappers active.

## 2. Import and registration repointing

Implements design Decision 2: import repointing instead of worker rewrite.

- [x] 2.1 [P] Change the defaultCache import in src/app/sw.ts from @serwist/next/worker to @serwist/turbopack/worker, leaving every other line untouched. Done when the worker compiles and the requirement "Runtime caching semantics preserved across the integration swap" holds (spot-check: the /api/auth exclusion is present in the built worker).
- [x] 2.2 [P] Change the SerwistProvider import in src/app/serwist-provider.tsx from @serwist/next/react to @serwist/turbopack/react. Done when the provider renders without warnings in dev.
- [x] 2.3 [P] Update the service worker registration URL in src/app/layout.tsx from /sw.js to /serwist/sw.js. Done when a fresh client registers the worker at the new URL with scope "/", satisfying the requirement "Root-scope registration from the new worker URL".

## 3. Dependency and script swap

- [x] 3.1 Update package.json: remove @serwist/next, @serwist/cli, and concurrently; add @serwist/turbopack and esbuild; set dev script to "next dev" and build script to "next build"; run pnpm install to refresh the lockfile. Done when pnpm install completes with no unmet peer warnings introduced by this change.
- [x] 3.2 Delete serwist.config.js and verify no tracked public/sw.js or public/sw.js.map exists (git ls-files public/sw.js public/sw.js.map returns nothing); confirm .gitignore still covers both paths. Implements design Decision 4: stop producing public/sw.js. Done when the working tree has no serwist CLI remnants.

## 4. Verification and release artifacts

- [x] 4.1 Run pnpm verify and pnpm verify:all; both green. Smoke-test dev: start next dev with a placeholder MONGODB_URI, request /serwist/sw.js, and confirm a compiled worker is returned; edit src/app/sw.ts trivially and confirm the next request reflects it, then revert the edit.
- [x] 4.2 Compare precache manifests to confirm the requirement "Install-time precache covers static assets only": extract the precache entry list from the built worker and confirm it contains all /_next/static chunks and public assets present in the previous configurator output, with exactly the ten prerendered navigation-shell HTML routes absent (expected 104 entries). This validates design Decision 3: accept the navigation-shell precache delta. Record the count in the PR description.
- [x] 4.3 Write the changeset (patch, heading "### Changed", domain "#### PWA") describing the two observable deltas: the worker URL moving to /serwist/sw.js and navigation shells moving from install-time precache to first-visit runtime caching. Commit it with the change.
- [x] 4.4 Browser-verify the worker on a running instance (Playwright against next dev or the PR's Vercel preview; public pages only, no auth needed): (a) the service worker registers from /serwist/sw.js, activates, and controls the page with registration scope "/" — satisfies the fresh-client scenario of the requirement "Root-scope registration from the new worker URL"; (b) after visiting /auth/sign-in while online, an offline reload still serves the page, while a never-visited public page fails offline — satisfies the nav-shell scenario of the requirement "Install-time precache covers static assets only". Record both checks in the PR description.
- [x] 4.5 Add scripts/assert-sw.mjs asserting the built worker output: the prerendered route metadata carries content-type application/javascript and service-worker-allowed "/", and the precache manifest contains no prerendered HTML navigation documents while containing the build's static chunks and public assets. Chain it into the verify:all script after the app build so CI guards the build-facing scenarios of the pwa-service-worker spec on every PR. Done when pnpm verify:all fails if either assertion breaks.

Post-deploy manual verification (per design Migration Plan, not blocking archive): the authenticated API offline fallback and the existing-client migration from /sw.js are verified on production by Andrew after the v0.14.1 release.
